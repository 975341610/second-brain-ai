Agent: 我复查了一下，上一轮自动修复没有产出有效结果，问题还没真正处理掉。我现在继续深入排查连线失效的根因，先把它修好再跟你回报。
Agent: 找到高概率根因了：上次为了把箭头方向“看起来更顺”，把 `handleConnect` 里的 `source/target` 真反转了，这很可能直接破坏了 ReactFlow 的连接校验，所以才会出现“拖得出来但连不上”。我现在按正确方案修：保留真实连接方向，只调整箭头呈现，不再篡改连接端点。
Agent: create_task(traecli: 修复画布无法连线)
