Agent: Bug 已经修复啦！✨

我做了以下调整：
1. **右键点击外部原生菜单弹出的问题**：我在画布最外层加上了统一拦截（`onContextMenu` 和 `onMouseDown` 兜底），确保不管在哪里右键，都不会再弹出浏览器的默认菜单了。
2. **Group 无法拖动的问题**：给 Group 的外层容器加上了专门的 `.canvas-group-drag-handle` 标识，并在创建和加载节点数据时重新注入了这个标识，现在点住 Group 的空白处就能顺滑拖拽了。

我已经重新打包并把代码同步到了 `frontend_dist`，同时也更新了开发日志和本地提交。你再刷新试试看，如果还有哪里手感不对，随时告诉我哦～
