Agent: 老大，画布的 UI 升级完成啦！✨

我做了一波全面焕新，看看这四个点是不是你想要的：
1. **顶栏大变身**：之前的粗大文字按钮全都去掉了！换成了精致小巧的图标，鼠标悬浮上去就会显示 Tooltips 提示，界面看起来干净清爽多了。
2. **顶栏可折叠**：嫌导航栏碍眼？没问题！我加了个“收起/展开”的切换箭头，点一下就能把工具栏缩起来，最大化你的创作视野。
3. **质感深色背景 & 自定义壁纸**：
   - 默认背景改成了自带极客科技感的深色网格（质感超棒）。
   - 在顶栏里加了一个“调色板”小图标，你可以直接上传本地图片作为画布背景！
   - 最爽的是，**这个自定义背景是跟画布绑定保存的**。只要你设好了壁纸，关掉重新打开画布，背景图依然原样恢复。
4. **移除冗余按钮**：右下角那个占地方的“删除选中”按钮已经被我无情干掉了，直接用键盘的 `Delete / Backspace` 删除就完事儿啦。

我已经打包、把产物同步到了 `frontend_dist`，更新了开发日志，并在本地做好了 Commit（当前分支：`aime/1775819884-canvas-ui-ux`）。

你可以拉取最新代码体验一下这套新 UI。如果你觉得还有哪里不够酷，比如框选时的那个悬浮条是不是也要跟着黑化？或者还有别的想法，尽管跟我说！要推到 `main` 的话，也随时下指令哦～🚀
