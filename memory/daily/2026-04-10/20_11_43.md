Agent: 老大，第一轮的“体检”结果出来了！确实抓到了几个致命的性能隐患：

1. **Base64 滥用**：在情绪板（Moodboard）和习惯打卡（HabitTracker）组件里，拖拽图片还是会被转成巨大的 Base64 塞进数据里，这会导致文件越来越大，最终肯定会爆掉 LocalStorage 或者把数据库塞满。
2. **FastAPI 阻塞 Event Loop**：后端 `routes.py` 里有很多接口明明定义成了 `async def`（这会让 FastAPI 在主线程里跑），但里面却执行了**同步的数据库查询和文件读写**。这在高并发或者大文件上传时，会直接把整个服务的响应卡死！
3. **无分页的大数据传输**：现在的 `GET /notes` 接口会把所有笔记的全部内容（包括几万字的正文 `content`）一次性全拉出来。这不仅让查询变得巨慢，还会让前端内存吃紧。
4. **前端频繁无效保存**：画布里现在的防抖虽然有，但是因为你点选卡片、拖动哪怕没改变位置，ReactFlow 的一些内部状态（比如 `selected`）也会触发文档变化，导致不停地向后端发送无效的保存请求。

我这边已经整理好了全套的手术方案，但在动刀之前，需要你拍板确认两个选择：

**决策 1：获取笔记列表（`GET /notes`）的改造方式**
- **方案 A（推荐）**：彻底分离。列表接口只返回标题、标签等摘要信息（绝对不带 `content` 正文，加上分页）。需要看某篇笔记时，单独调一个新的接口获取那篇笔记的完整内容。
- **方案 B（偷懒但留坑）**：列表接口还叫那个名字，默认把 `content` 设为空字符串，只有前端传 `?include_content=true` 时才返回正文。

**决策 2：习惯打卡（HabitTracker）上传图片失败时**
- **方案 A（推荐）**：如果传服务器失败了，就直接提示用户“上传失败”，坚决不让它回退成 Base64 污染文档数据。
- **方案 B**：允许它妥协，转成 Base64 存下来，但是加上严格的体积限制（比如只能 50KB 以下）。

你选一下 A 还是 B，我就立刻开始动手写代码，并给你生成一份详细的体检修复报告！🚀
