Agent: 四项体验级 Bug 全部被我亲手捏掉啦！代码已经本地 commit 完毕。

给你盘点一下这波舒爽的改动：
1. **Group能随意拖动了**：我给标题栏左边加了个小把手图标（`LayoutGrid`），并且把旁边那些输入框和按钮的误触拖拽给屏蔽了。你现在点住左边那个小把手或者标题栏空白处，随便拖。
2. **卡片能轻松移出 Group 了**：重写了脱离判定逻辑（抓取真正的绝对坐标）。你现在只需要按住卡片往外一拽，只要它的中心点离开了 Group 的虚线大框，它就会自动“净身出户”（断开 parentId 并停在你松手的地方）。
3. **右键菜单不再双重遮挡**：通过在画布、卡片、甚至连线上的右键事件里强行注入了 `preventDefault`，那个碍事的浏览器原生右键菜单彻底跟你说拜拜了！
4. **Group收纳后虚线框完美跟着变小**：原来只是卡片隐藏了但框架没缩。现在你点一下折叠，不仅仅卡片消失，连带着外面那个大虚线框也会干脆利落地缩成一条 **40px 高** 的胶囊。再点展开，虚线框又会自动撑开恢复原样。

所有产物都已经通过 `npm run build` 打包并同步到了 `frontend_dist`，开发日志也记录在案了。

快去狂按 F5 刷新体验一下这套顺滑无渣的 Group 分组系统吧！✨如果有哪里不符合直觉或者手感不对，随时滴滴我！
