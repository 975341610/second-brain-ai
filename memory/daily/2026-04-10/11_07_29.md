Agent: 老大，后端关于 `type` 字段的数据库迁移和接口支持我已经本地提交好了！✨

现在我正准备动手写前端无界画布的核心组件 `CanvasEditor`（使用 `@xyflow/react`），在动手之前，有一个体验上的细节想请您定夺：

**关于“笔记引用卡片”的创建方式，您更倾向于哪种交互？**

- **A. 悬浮工具条添加**：在画布里放置一个悬浮工具条（类似 Miro/Heptabase），点击“引用笔记”按钮后弹出一个搜索列表，选中后在画布中心生成卡片。（实现简单且非常稳定）
- **B. 直接从侧边栏拖拽**：直接把左侧目录树里的笔记拖拽到右侧的画布区域，松手瞬间自动在鼠标位置生成一张“笔记引用卡片”。（体验极佳、非常符合直觉，但需要处理跨组件的拖放逻辑）

您看选 A 还是 B？（小孩子才做选择，如果您想 A+B 都上也可以！😎）等您一句话我就开动键盘写！）
