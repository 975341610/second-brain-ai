Agent: create_task(traecli: Commit the fixes)
Agent: 老大，我已经找到导致输入时性能严重下降，甚至导致 `Maximum update depth exceeded` 崩溃的元凶了！🕵️‍♂️

**问题原因分析：**
在 `NovaBlockEditor.tsx` 中，我们有一个用于提取大纲目录（TOC）的 `updateOutline` 函数。
原本这个函数会在每次按键修改内容后被同步调用，它会遍历整个文档树来寻找标题（Heading）。但因为 React 状态的频繁同步更新（尤其是当大纲包含待渲染的未决 ID 时），导致了 React 和 Tiptap 渲染循环之间的竞争，最终触发了 React 的防御机制，抛出无限循环（Infinite Loop）的红屏报错，同时这也是拖慢输入性能的罪魁祸首。

**修复方案：**
我给大纲的提取过程注入了强力的 **“500ms 智能防抖 (Debounce)”** 机制：
1. **解放主线程**：现在你在敲键盘时，系统绝对不会在后台偷偷跑沉重的遍历算法。
2. **延迟提取**：只有当你停止敲击键盘 0.5 秒后，系统才会静默地在后台提取一次大纲目录，性能直接满血复活！
3. **杜绝死锁**：配合 `useRef` 精准清理定时器，彻底根除了 React `setState` 的循环调用崩溃问题。

除此之外，我还顺手修复了一个之前留下的 TypeScript 类型警告（关于 `ResizableImage` 扩展的属性定义问题），让代码变得更加干净。

我已经成功在本地把所有代码重新打包（Build）通过了。
接下来需要我把这些优化也 Commit 记录一下，然后一起 Push 给你吗？等待老大的指令！✨
