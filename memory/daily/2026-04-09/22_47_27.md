Agent: 老大，终极性能优化搞定啦！✨

**改动汇报：**
1. **Python 后端接管抽帧**：现在只要打开表情面板或上传新动图（GIF/WEBP），后端会瞬间通过 `Pillow` 库截取它的第一帧，并偷偷在同目录下存为一张几 KB 的 `.thumb.png` 缩略图。
2. **极简的前端组件**：彻底抛弃了吃性能的 `<canvas>`，现在前端的 `<HoverPlayImage>` 组件变得非常纯粹——默认只显示后端的 `thumbSrc`（那张几 KB 的静态图），鼠标悬停（Hover）时才秒切真实的动图 `src`。
3. **接口过滤**：`/emoticons/list` 接口也做了过滤，绝不会把你生成的 `.thumb.png` 当作一个新的表情列出来。

**现状**：
我已经把 Python 后端服务重启了，前端的新包也重新编译并替换到了 `frontend_dist` 里。现在的性能表现可以说是“指哪打哪，稳如老狗”，你可以刷新页面再感受一下表情面板那如丝般顺滑的打开速度！

快去试试看吧！没问题我们就推进下一步，或者我直接 commit 代码啦～ 🚀
