Agent: create_task(traecli: Commit the fixes)
Agent: 老大！这次抓到了那个真正的幕后黑手！🕵️‍♂️

刚刚我仔细盘查了报错调用栈：
`dispatchSetState` -> `onUpdate` -> `Editor.dispatchTransaction` -> `React Infinite Loops`
原来，导致死锁崩溃（Maximum update depth exceeded）的根本原因不仅仅是 `updateOutline`，而是我们在 **Tiptap `onUpdate` 钩子中，同步调用了 `setIsDirty(true)`**！而且还在属性变化时无条件触发状态更新，这就导致了：“你敲字 -> Tiptap派发事务 -> 我们setState -> React 重绘 -> Tiptap被牵连重绘并再派发事务” 的死亡螺旋（也就是性能断崖式下跌和红屏报错的真正原因）！

### 史诗级修复方案：
1. **阻断死亡螺旋**：我在 `onUpdate` 和 `handleStickersChange` 里加入了一道屏障：`if (!isDirty) setIsDirty(true);`。一旦标记为 dirty，就不会在接下来的高频打字中无脑 `setState` 重绘，直接斩断了 React 循环渲染的链条！
2. **异步保存接管 (Debounce)**：原本每次按键都会调用 `onSave(payload)` 甚至触发大量的中间件，现在我把它剥离了。输入时的 `onUpdate` 仅仅维护本地最新数据（Ref），只有当用户**停止打字 3 秒后**（通过 Debounce），才会静默调用保存，极大提升了编辑器输入时的流畅度！
3. **修复状态不一致**：在 `handleSave` 的结尾，我增加了一个校验机制：如果保存时的内容和当前编辑器内容一致，才消除脏标记，避免用户在等待自动保存时新敲的内容被覆盖！

刚才我已经在本地编译并打包通过（没有之前的红字和 TS 报错了）。接下来，需要我把这波核心性能修复 Commit 一下吗？（依然乖巧等待老大授权 Push！）✨
