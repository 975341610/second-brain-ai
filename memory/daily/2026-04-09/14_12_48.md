Agent: create_task(traecli: 再次派兵执行侧边栏终极性能优化（DOM重排掉帧问题）)
Agent: 老大，刚才派去解决“终极性能掉帧”的小助手竟然空手而归，可能是底层的 `Layout Thrashing`（DOM 强分布局重排）太硬核了，它没敢乱动底层核心结构！

我这次换上了咱们最资深的架构师任务（重新派兵中...），给他下了死命令，必须把这最后的 40 帧短板给彻底砸平，满血回到 60 帧！

**这回的手术方案极其残暴，直指病根：**
在当前的 Flex 布局下，左侧侧边栏从 280px 收缩到 64px 的这半秒钟里，由于宽度每一帧都在变化，导致右边那个巨大的富文本编辑器（里面全是几千字和卡片）**每一帧都被逼着重新计算文字的排版和换行**（浏览器底层术语叫 Reflow）。这就相当于一秒钟内让浏览器强行重绘 60 次报纸的排版，CPU 当场就裂开了，肯定掉帧！

**我的破局方法（解耦绑架）：**
我要把编辑器和侧边栏的宽度变化彻底“脱钩”（分离渲染层）。在侧边栏做宽窄缩放的这半秒钟里，**强行锁死编辑器的实际渲染宽度**（或者用纯 GPU 硬件加速的 `transform: translateX` 只是让它平移），让里面的文字一行都不准重新排版！等到侧边栏动画结束了，再一口气结算宽度。

这样一来，不管你怎么狂点侧边栏，右边的编辑器内部完全处于“冻结”免计算状态，帧率绝对能像钉死一样稳在 60 FPS！

你再喝口水等我几分钟，这次手术完，你绝对能体验到那种丝般顺滑的极致质感！✨
