User: [User New Message]
1.双向链的侧边栏面板不能正常显示关联数据
2.点击可以跳转了，但是如果跳转的笔记改名了话，引用的笔记里不会更新，再次点击后就会到一个新的未知笔记页面

[Current Datetime]
Thursday, 2026-04-09 19:28


Agent: 老大，我已经完成了针对您反馈的这两个问题的深度修复：

**1. 双向链面板不显示数据的问题（离线化改造）**
我将侧边栏双向链接面板的请求模式，从之前发起 `fetch` 到 Python 后端（由于您跑的是独立前端端口导致跨域/数据不一致报错 404/500）改为了 **“纯前端内存提取”** 的离线优先模式。
现在，侧边栏会直接读取存在浏览器内存（`window.novaNotes` 全局状态）中的所有笔记，通过正则表达式直接匹配 `data-id`。彻底解决了无法显示数据的问题。

**2. 跳转笔记改名不更新的问题（响应式同步改造）**
对于 `[[笔记名]]` 这种内联在编辑器里的胶囊 UI，我为其重构了实时同步逻辑：
当笔记被重命名时，触发全局事件 `nova-notes-updated`，此时带有该 `data-id` 的所有引用胶囊会自动去全局状态中拉取最新的 `title` 并实时更新自己。另外，跳转时的 Bug（找不到目标笔记）也已经通过类型安全（严格转为 `Number`）一并修复了。

同时我也在您的环境里重新执行了 `npm run build` 打包。您可以刷新页面测试一下改名与点击跳转的功能。如果运行完美，我们就可以推代码或者进行下一步啦！
