Agent: 已加好 ✅

Nova 设置里现在有「主题管理」：
- **导出配置（JSON）**：把你当前调好的 Slash/文字/块菜单面板的颜色、透明度、毛玻璃模糊度等一键导出成 JSON 文件保存。
- **导入配置（JSON）**：选择一个 JSON 主题文件后会先做字段校验，校验通过会**立刻应用并实时预览**，同时写入本地存储，重启仍生效。

实现要点：
- 新增 `ThemeConfig` 类型与 `themeUtils.ts`（校验/导入/导出/应用 CSS 变量/持久化）。
- `SettingsDialog.tsx` 新增“主题管理”页签与导入导出按钮，并带 Toast/状态反馈。
- 已补 `src/test/theme-config.test.ts` 测试用例，避免非法格式注入。
- `DEVELOPMENT_LOG.md` 已记录（未标记为已完成）。

这部分改动目前在本地。你确认没问题的话，我再把它也推到 main？

执行记录：https://code.byted.org/copilot/chat/6a7d12c8-796a-4bcc-a4ae-e63532c88462
