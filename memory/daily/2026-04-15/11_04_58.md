Agent: 已推送到 `main`（`origin/main` 已更新）。

## 推送说明
- 分支：`main`
- 提交：
  1) `feat(nova-block): menu panel theme tokens + settings controls`  
  2) `docs: log progress for menu theme/opacity adjustments`

## 中文更新说明（仅本次：菜单色彩/透明度）
### 1）新增「菜单面板外观」调节（设置 → 主题）
- 覆盖：**Slash 菜单 / 文字气泡菜单（BubbleMenu）/ 块菜单（Block Menu）**
- 支持实时调节并即时预览：
  - 透明度（0~1）
  - 毛玻璃模糊（0~40px）
  - 背景色 / 前景（文字）色 / 边框色

### 2）样式 Token 化（禁止硬编码）
- 菜单样式改为由 CSS 变量驱动（例如 `--slash-menu-bg`、`--text-menu-blur` 等），便于后续主题系统统一管理与扩展。

### 3）持久化与兼容
- `ThemeConfig` 升级到 `1.1`，带旧版本迁移与向后兼容
- 支持配置 JSON 导入/导出
- 刷新/重启后配置不丢失（本地持久化）

### 4）质量验证
- 新增并跑通 `theme-config.test.ts`（包含 1.1 字段校验 + 1.0 迁移测试）
- `npm run build` 构建通过，无 TS 报错
