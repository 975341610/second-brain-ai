User: [User New Message]
你推到main吧，中中文写好推送和更新说明

---

<system_notification type="skill_update">
Skills Removed:
- **deepwiki**

</system_notification>

[Current Datetime]
Wednesday, 2026-04-15 10:43


Agent: 老大，已经推到 `main` 了（远端 `origin/main` 已更新）。下面是本次 **推送说明 + 中文更新说明**，可直接作为 MR/Release Notes 使用：

## 推送说明（main）
- 已将近期修复分支内容合并到 `main`
- 已执行 `git push origin main`

## 更新说明（Changelog）
### 1）背景纸（Dot / Line / Grid）
- 修复「背景纸菜单点不动」：从悬停触发改为点击触发，并提升菜单层级（z-index），避免被贴纸/覆盖层遮挡。
- 修复「频繁切换背景纸白屏」：背景纸组件在 `none` 状态不再卸载 DOM，而是透明化处理，避免与 Tiptap/React DOM 同步冲突导致崩溃。

### 2）编辑器稳定性
- 修复背景纸切换时可能触发的状态同步竞态：优化渲染/保存时序，降低与编辑器事务同时更新引发的异常概率。

### 3）云端（Linux）AI 开关
- 修复云端 AI 开关打不开/超时：在非 Windows 环境跳过 Windows 专属 `ensure_ollama.py` 流程，并将子进程调用改为异步非阻塞，避免阻塞 FastAPI 事件循环。

如果你想把这段更新说明同步到 `DEVELOPMENT_LOG.md` 的某个固定格式里，我也可以按你的模板再整理一版。
