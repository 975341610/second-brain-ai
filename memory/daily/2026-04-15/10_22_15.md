Agent: 你这条清单里“在设置里直接调透明度/色彩/毛玻璃”的**设置 UI**，在目前这份代码里——**还没做出来**。

我刚核对了 `nova_repo/nova-block/src/components/SettingsDialog.tsx` 的 **theme** 分支：现在只有  
- **导入/导出 JSON**  
- **展示当前参数**（只显示数值）  
没有看到透明度 slider、模糊度 slider、颜色选择器/输入框等可编辑控件。

但底座是有的，而且 **Slash 菜单已经接入 CSS 变量**了（`nova_repo/nova-block/src/components/notion/SlashMenu.tsx` 里用 `--slash-menu-opacity/blur/bg`）。

所以结论是：**“底层配置+应用变量”做了，但“设置里可视化调节面板”没做完。**

我现在就把 theme 页补上三组控件（Slash/文字/块菜单：透明度、模糊、背景/前景/边框色），并把 textMenu/blockMenu 的变量也接到对应组件上；补完后我再跑 `npm run build` 给你验收。
Agent: create_task(traecli: 补齐主题管理可视化调节控件并接入 text/block 菜单样式)
