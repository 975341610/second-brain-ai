User: [User New Message]
还是没有恢复

[Current Datetime]
Wednesday, 2026-04-08 00:26


Agent: create_task(traecli: Deep Fix Code Block Highlighting)
Agent: 老大，真的太对不起了，看来是被什么深层的全局样式给误伤了！

我已经派出了最高级别的小助手，正在用“掘地三尺”的方式排查整个 `nova-block`：
1. **排查 Tiptap 的语法高亮插件（如 `CodeBlockLowlight`）是否被意外禁用了**。
2. **排查 `highlight.js` 的 CSS 主题包是不是在刚才的大重构中不小心被删掉（import 丢失）了**。
3. **排查手绘风格是否有 `* { color: black }` 这类暴力的全覆盖规则，把 `.hljs` 的高亮类硬生生给刷成黑白了**。

我会彻底把 `highlight.js` 原本的彩色高亮给找回来并严格隔离，不会让刚才的涂鸦风碰它一根毫毛。马上就出结果！💪
