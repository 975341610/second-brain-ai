User: [User New Message]
加粗、內联代码、引用的字被变成白色了

[Current Datetime]
Wednesday, 2026-04-08 00:18


Agent: create_task(traecli: Fix White Text for Bold, Code, and Quote)
Agent: 收到，老大！我已经明确把目标锁定在**加粗（Bold）、内联代码（Inline Code）和引用块（Blockquote）**这三个“重灾区”上了。

这通常是咱们上回引入某些深色模式（Dark Mode）样式或者是手写边框样式时不小心把文字的 `color` 给覆盖成了白色（`#fff`）导致的。

我立刻让小伙伴重点排查 `novablock-core.css` 里对 `<strong>`、`<code>` 和 `<blockquote>` 的样式定义，把它们强行按回到深色（黑色或深灰），保证在白底下清清楚楚！稍等片刻，很快搞定！✨
