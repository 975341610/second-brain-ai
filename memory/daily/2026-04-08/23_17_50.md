Agent: 老大，终于揪出真正的“幕后黑手”了！！🎯

这回我亲自上阵，彻底查清了这几天**表情包点不出来**的千古谜案：

**究竟是什么在捣鬼？**
之前为了图省事，小弟把表情包面板 `<EmoticonPanel>` 直接塞进了 Tiptap 原生的悬浮菜单 `<BubbleMenu>` 里面！
这个 `<BubbleMenu>` 是个极其霸道的组件：一旦你点击里面的任何按钮（导致编辑器选区光标丢失），它就会认为“你不想选文字了”，然后**瞬间自爆销毁**。
所以真相就是：**你点表情按钮的瞬间，整个菜单连同里面的表情面板，在 1 毫秒内被连锅端掉了！** 肉眼看起来，就是永远点不出面板。

**我的终极重构手术（已完成）：**
1. **断绝父子关系 (Detached)**：我直接把 `<EmoticonPanel>` 从 `BubbleMenu` 的 DOM 树里强行挖了出来！
2. **全局独立挂载**：把它放在了整个编辑器最外层、和菜单平级的地方，给它披上了 `fixed top-1/2 left-1/2 z-[99999]` 的霸体护甲（现在它会像一个高贵的弹窗一样，稳稳地居中显示在屏幕上，绝对不会被任何菜单的死亡所牵连）。
3. **点击冒泡阻断**：在表情按钮上加了 `e.stopPropagation()` 和 `e.preventDefault()` 双重保险，点击它再也不会触发编辑器的失焦检测。

**现场战况：**
我已经执行过一遍打包编译，确定完全没有报错，并且这次的“脱离 BubbleMenu”重构日志已经老老实实写进了 `DEVELOPMENT_LOG.md` 里！
**（保证没有执行任何 Git Push 操作）**

你现在可以立刻刷新刚刚那个 [前端预览地址](https://5173-1386dd6b88a446e9af180a631a881ac6-cube-kubestrato-online7.code-server.strato-https-proxy.bytedance.net) 看看！！这次点那个笑脸按钮，应该能非常霸气地弹出一个居中表情包面板了！😎
