User: [User New Message]
还有一堆bug和报错
Failed to load resource: the server responded with a status of 404 (Not Found)
8765-1386dd6b88a446e9af180a631a881ac6-cube-kubestrato-online7.code-server.strato-https-proxy.bytedance.net/api/media/music-library:1  Failed to load resource: the server responded with a status of 404 (Not Found)
2MusicContext.tsx:196 Failed to fetch music library: Error: {"detail":"Resource not found"}
    at Object.listMusicLibrary (api.ts:188:29)
    at async MusicContext.tsx:176:20
（匿名） @ MusicContext.tsx:196
2StickerPanel.tsx:29 Failed to fetch stickers: SyntaxError: Unexpected token '<', "<!doctype "... is not valid JSON

[Current Datetime]
Wednesday, 2026-04-08 16:24


Agent: create_task(traecli: 彻底修复后端 API 404 及 HTML 污染问题)
Agent: 啊！居然还在报错！太对不起了老大，这说明我刚才的修复是个“半吊子”工程，或者根本就没把病根给切掉！😭

那个该死的 `<!doctype...` 再次出现，说明后端的“黑洞路由”依然存在，而且它还在强行把所有找不到的 API 统统变成网页（HTML）塞给前端！不仅贴纸报这个错，你原来的音乐库甚至连 404 都报了（连被黑洞接住的资格都没有，直接就断了）！

**这是严重的底层路由注册断裂！我必须得下重手了！**

我马上让小弟带着“防爆盾”进去，这次不修好不出来了：
1. **直接拔除“HTML 污染源”**：这次我必须找到那个把 API 变成网页的兜底逻辑（`main.py` 里的 SPA fallback），狠狠地加上一条“免死金牌”——**只要你名字里带 `/api/`，就算死了也得给我报 404 JSON！绝不允许给我变网页！**
2. **彻查路由大动脉 (`routes.py`)**：我要拿着放大镜去找 `app.include_router()` 这个最关键的桥梁，看看是不是被谁注释掉了，或者是前缀写劈叉了（比如一边挂载 `/api` 一边路由里又带了一个 `/api` 变成 `/api/api/...`）。
3. **强制创建默认文件夹**：万一是你还没建 `data/stickers/` 和 `data/music/` 导致的呢？我直接在后端启动时强行把它俩建出来，防一手万一。

老大，你在前面喝口茶稍安勿躁，我这就让他把后端开起来，**用 `curl` 亲手发请求测试**。不拿到真正的 JSON 数据绝不跟你报喜！马上回来！🚀
