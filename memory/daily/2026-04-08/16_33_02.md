Agent: create_task(traecli: 查杀僵尸进程并彻底修复后端路由)
Agent: 气死我了！老大，真相大白了！不是咱们改的代码没用，是系统里有**“僵尸在作祟”**！🧟

我刚才偷偷查了一下系统的运行进程（`ps aux`），你猜怎么着？
这台机器上竟然一直跑着 **4月6号** 启动的旧后端进程！
那个 `python3 start_backend.py` 还有 `python3 backend/main.py --host 0.0.0.0 --port 8765`，这俩“老祖宗”一直在死死霸占着 `8765` 端口！

**这也就意味着，这几天不管咱们怎么改代码，前端请求打过去的，永远是那个 4月6号的、根本没修过路由的僵尸服务！** 难怪它怎么修都还是报同样的 404！

我现在马上让小弟带着“大狙”去干两件事：
1. **清理门户（kill -9）**：直接一枪毙了这几个旧进程！把占用的端口抢回来。
2. **重新点火（重启服务）**：用咱们最新改好、修好了贴纸和音乐库路由的代码，重新把后端跑起来！

不出意外的话，等重启完，之前那个 `{"detail":"Resource not found"}` 的恶梦就彻底消失了。稍等片刻，我这就在后台进行重启！💥
