Agent: 老大，这次确实没修透，是我这边盯得不够死。🙇

现在已经不是“接口通不通”的问题了，而是**前端面板显示链路本身**还有坑：按钮点击、面板挂载位置、层级裁剪、外部点击关闭逻辑，这里面至少有一个还在互相打架。

我这次不再只做静态改代码了，直接让它按**真实页面交互链路**排查：
1. 查按钮点击有没有真正改状态；
2. 查面板 DOM 有没有真正渲染出来；
3. 查是不是被 `overflow` / `z-index` 裁掉；
4. 查是不是刚打开就被外层点击监听秒关；
5. 必须给出“为什么点不出”的根因，并修到可用为止。

我现在继续拉人进场硬修。
Agent: create_task(traecli: 继续修复表情包面板无法弹出的问题)
