Agent: 老大，针对刚才发现的两个严重 Bug，我已经紧急完成了精准修复！✨

这次是“手术级”的调整，重点解决了定位参考系和事件冒泡的问题：

### 🔧 修复详情：

1.  **修复工具条“乱飞”：坐标系大重构**
    *   **问题**：之前用的是 `fixed` 定位，它是相对于浏览器窗口的，所以页面一滚动或者布局稍有变化就飞了。
    *   **解决**：我把工具条改成了 `absolute` 定位，并重新编写了坐标计算逻辑。现在它会**相对于编辑器容器**进行像素计算（`coords.top - containerRect.top`）。无论你如何滚动页面，工具条都会像长在 Block 上一样，死死地跟在行左侧。

2.  **修复菜单点击无响应：拦截焦点丢失**
    *   **问题**：点击菜单项时，编辑器会因为失去焦点（Blur）而自动关闭菜单，导致点击事件还没来得及传给 action 就消失了。
    *   **解决**：我把点击触发改成了 `onMouseDown` 并配合 `e.preventDefault()`。这样在点击的一瞬间，系统会**锁住编辑器的焦点不放**，确保指令（比如“插入标题”或“新建子页面”）能够 100% 被执行。

3.  **菜单滚动同步**
    *   现在的 `/` 菜单也改为随编辑器内容同步滚动了，交互感更像 Notion 原生体验。

### ✅ 验证建议：
*   **吸附测试**：鼠标悬停在不同行，看 `+` 和 `::` 是否精准对齐行首。
*   **滚动测试**：在长文档中滚动，看工具条是否保持在原位。
*   **点击测试**：输入 `/` 后用鼠标点击“标题 1”，或者点击行首的 `+` 选一个选项，看是否能正确插入。

老大，请再试一下！这次的稳定性应该已经完全拉满了！🚀
