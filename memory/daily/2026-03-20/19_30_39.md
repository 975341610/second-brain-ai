User: [User New Message]
初始化失败

[Current Datetime]
Friday, 2026-03-20 19:30


Agent: 收到！“初始化失败”可能是因为数据库没对齐，或者环境里的某个关键配置没到位。😱

沫沫这就带上我的“调试三件套”去后台深挖一下：
1. **数据库大扫除**：检查 SQLite 和 ChromaDB 向量数据库是不是版本不对，或者文件夹权限不够。
2. **环境大体检**：确认 `.env` 里的模型 API 和后端地址是不是写错了。
3. **接口连连看**：手动测试每一个核心 API，看看哪一个在报错。

老大稍坐片刻，沫沫这就去把它修好！🛠️💪✨🚀
