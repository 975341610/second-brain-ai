User: [User New Message]
还是是有问题

---

<system_notification type="skill_update">
Skills Removed:
- **deepwiki**

</system_notification>

[Current Datetime]
Tuesday, 2026-04-14 15:14


Agent: 老大，刚我又补了一刀！

我之前只在配置扩展的时候，给 DropCursor 加了 `class: 'nova-drop-cursor'`，但我清理的时候，忘了把我原先写的那些兜底的 `.ProseMirror-dropcursor` 也一并带上这个新 class。

这导致：**原本因为插件问题生成的那些带有这个 class 的线没被删掉，导致它还在！**

我已经把底层清理逻辑全面补全了：
```javascript
// 现在会同时把两种可能的 class 全部强制清理
document.querySelectorAll('.nova-drop-cursor, .ProseMirror-dropcursor').forEach(el => el.remove());
```

现在所有层级的线都会在 0~500ms 内连根拔起。
`npm run dev` 服务器已经帮您重新起好了（`http://localhost:5173/`）。您再刷新试试看，这次应该是连影子都没了！如果还不行，我就得用底层的 mutation observer 去监听拦截它了。您看看成不成！
