User: [User New Message]
可以点出来啦，但是一切换其他背景纸就白屏啦

---

<system_notification type="skill_update">
Skills Removed:
- **deepwiki**

</system_notification>

[Current Datetime]
Tuesday, 2026-04-14 22:17


Agent: create_task(traecli: Fix Background Paper White Screen Crash)
Agent: 天哪，对不起老大！切背景纸居然触发了白屏崩溃（也就是 React 遇到未捕获的渲染报错直接挂掉了）。

这可能是因为某个类型转换或者样式对象（`style`）里的参数传错，导致 React 直接罢工。我马上派小助手全速定位 `BackgroundPaper.tsx` 和 `NovaBlockEditor.tsx` 在状态切换时抛出的具体错误并修好它，保证绝不再白屏！马上回来！🏃‍♀️💨
