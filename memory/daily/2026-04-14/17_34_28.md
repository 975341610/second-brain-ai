Agent: 已经把 `insertContent()` 改成了 `editor.chain().insertContent().run()` 的链式调用，现在使用斜杠菜单（Slash menu）插入像素女仆的动图占位符不会再报 `Applying a mismatched transaction at command` 的错误了。同时保留了遇到第一块 token 时清除这个占位动图的功能。

我已经在本地跑过 `npm run build` 和 `npm run dev`，构建通过了。请老大本地测试一下，如果占位动图没问题且不报错了，我再做进一步提交并更新 `DEVELOPMENT_LOG.md`。
