Agent: create_task(traecli: 修复 Drag Handle 拖拽冲突与 Spellcheck 残留装饰线)
Agent: 收到老大！我已经把远程仓库最新代码拉取下来并覆盖本地了。刚才看了您的最新提交，Slash Menu 和 Drag Handle 的漂移修好了，太棒了！

不过您提到的这三个新问题，我立刻安排后台进程进行深度排查和修复：
1. **块手柄一拖动就弹菜单**（应该是 `mousedown`/`click` 冒泡或者 drag 冲突导致）
2. **黑色指示线显示 Bug 且拖拽后不消失**（应该是 Drop Cursor / Drag 的清理事件漏掉了或者 CSS 渲染卡住）
3. **错别字一键采纳后，红色波浪线不消失**（这是 `AISpellcheck.ts` 在替换文本后，没有把该位置的 ProseMirror Decoration 清理掉）

后台代理 `traecli` 正在执行修复，我会严格跑通本地的 `npm run build` 和测试用例再给您汇报进展。稍等片刻！
