Agent: 老大息怒，刚刚的问题是因为原来直接用 `<img>` 字符串插入时由于缺少对应的 TipTap Schema Node 定义，所以渲染时只是一闪而过就被 TipTap 的内部过滤机制当作“非法节点”给清洗掉了（所以只有黑框一闪）。

这回我没有偷懒，正儿八经用 `@tiptap/core` 的 `Node.create` 注册了一个自定义行内节点 `aiLoadingPlaceholder` 专门来渲染和承载这个女仆图片，把它挂载到 extensions 里了。这下女仆占位图会作为 TipTap 原生节点稳定出现，并且流式生成一开始就会通过名字（`node.type.name === 'aiLoadingPlaceholder'`）安全地将其销毁。

本地重新跑了 `npm run build` 和 `npm run dev` 全通，云端预览我也拉起来了，麻烦老大帮忙再瞄一眼（`http://8bfdd3c16844.aime-app.bytedance.net`），这回没问题的话我就直接 Push 了！
