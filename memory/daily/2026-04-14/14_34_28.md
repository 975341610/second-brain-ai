Agent: 老大，我安排了深度检查任务彻底把剩下两个顽固问题也修了一遍，最新结果汇报：

1. **Drag Handle 拖拽与菜单冲突 (Notion 风格) - 确认修复 ✅**
   引入了 `dragInteractionRef` 追踪点击起点和时间，严格区分了“短按点击（弹菜单）”和“长按移动（纯拖拽不弹菜单）”。移动超过 4px 或按压超过 300ms 都会稳定判定为拖拽。
2. **Drop Cursor 黑线残留 - 确认修复 ✅**
   强化了全局清理机制，在拖拽结束的 `dragend`, `drop`, `mouseup` 事件里，采用 0~500ms 分层级循环清理策略，保证不管是底层自身逻辑延迟还是 React 渲染卡顿，黑色指示线都会被彻底拔除。
3. **拼写检查应用后红色波浪线不消失 - 确认修复 ✅**
   完善了 `AISpellcheck.ts` 里对 `removeError` 信号的拦截处理，点击修正后强行置空当前错误的 Decoration，不会再有任何视觉残留。
4. **所有剩余 TypeScript 编译报错 - 确认修复 ✅**
   补充了所有缺失类型和遗留死代码，`npm run build` 已可无痛绿灯通过。

这些问题您可以在本地开发环境（`npm run dev` 已经跑起来了）刷新页面去重手测试一下。要是还有遗漏，您随时骂我！
（详细修复代码过程可以在这里查看：[开发记录](https://code.byted.org/copilot/chat/1a3468ec-8db3-4efb-9afd-194d287001a2)）
