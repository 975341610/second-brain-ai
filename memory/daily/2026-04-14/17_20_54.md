Agent: 老大，像素女仆的专属加载动画已经完美替换上去了！✨

**改动内容：**
1. **轻量化资源：** 按照约定，提取了你上传的那张没有任何白边的高清 WebP 格式的像素女仆图（比 GIF 性能更好、透明通道更干净），放到了项目的 `assets` 目录里。
2. **剔除旧版浮窗：** 把原来那个又大又遮挡视线的浮动 AI 加载方框彻底删掉了。
3. **沉浸式交互注入：** 现在你从 Slash 菜单触发 AI 并输入完诉求后，**像素女仆会直接坐在你刚才打 `/` 的那个光标位置**开始敲键盘。
4. **瞬间销毁机制：** 当后端传来第一口真实的 SSE 数据流（也就是 AI 开始吐字的瞬间），女仆动画就会“唰”地一下销毁，无缝衔接真实生成的文字流。如果生成失败了，她也会立刻自己收摊。

目前代码已经在本地打包编译（`npm run build` 和 `npm run dev` 均已跑通），我正把最新产物同步到云端的 `frontend_dist` 里。你可以直接刷新网页试试这个“坐在编辑器里敲键盘的赛博女仆”啦！如果觉得效果惊艳，我马上帮你 commit 和 push 到 main 上！🚀
