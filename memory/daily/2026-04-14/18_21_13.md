Agent: 老大，Nova 的“笔记模板功能”我已经帮你写完啦！🎉

这次修改不仅完成了全套从前后端到数据库打通的逻辑，在 UI 和流程上也都做了完善：
1. **数据与接口层**：在后端（`db_models.py`, `repositories.py`, `routes.py`）新增了一套基于 SQLite 的模板存储逻辑；前端 API（`api.ts`）补齐了模板列表查询、创建与删除的方法。
2. **存为模板入口**：在编辑器顶部的 Toolbar（“贴纸模式”按钮旁边）新增了一个“存为模板” (`Copy` 复制图标) 按钮。当你写好一篇心仪的格式，点一下就能一键将其永久保存进模板库。
3. **模板选用与创建入口**：
   - 在侧边栏原来的“新建笔记”按钮旁边，新增了一个专门的“从模板新建”入口按钮（`Layout` 图标）。
   - 点击该按钮，或者点击编辑器的“存为模板”，都会弹出一个我精心手撸的 `TemplatePicker` 浮层组件。这个面板里会展示当前存在的所有模板，并可以直接选中它生成一篇全新的笔记，或者进行快速删除管理。

相关的改动细节我已经如实更新到 `DEVELOPMENT_LOG.md` 里面了，没有采用任何硬编码和丑陋的 Hack，确保可维护性。代码编译以及云端预览也全部通过！

现在云端预览（`http://8bfdd3c16844.aime-app.bytedance.net`）已经拉起来了，麻烦老大先去试玩一下：
- 试试把任意笔记存为模板
- 试试从侧边栏新建笔记时选中一个刚才的模板

如果有需要微调 UI 或者交互细节的地方随时告诉我，没问题的话我就可以安排合并推送到远端仓库啦！✨
