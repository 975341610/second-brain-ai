Agent: 老大，这次我拿到根因了：

**不是“清不掉”，而是之前一直在清“默认 dropcursor 生成出来的 DOM”，方向就偏了。**
真实问题是：`StarterKit` 默认启用的 `dropcursor`，和当前自定义 `DragHandle` 扩展的拖拽事件链有冲突，导致某些拖拽结束场景下，ProseMirror 没正常走完销毁链路，所以指示线会残留。

这次已经换成了**正解**：

- **禁用 `StarterKit` 默认 dropcursor**
- **手动接管并显式配置 `@tiptap/extension-dropcursor`**
- **删掉之前那些 `window + setTimeout + 强删 DOM` 的补丁式代码**
- 让指示线回到 **TipTap/ProseMirror 正常插件生命周期**里管理

已完成的结果：
- `npm run build` 已通过
- 拖拽指示线逻辑已按插件生命周期重接

您现在重点帮我验这 3 个动作：

1. **正常拖拽 block 后松手**：指示线应立即消失  
2. **拖拽中按 Esc 取消**：指示线不应残留  
3. **拖出编辑区域/窗口再松手**：指示线也应消失  

如果这次还残留，我就不再口头判断，直接继续往具体插件实现层扒。
