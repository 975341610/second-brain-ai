Agent: I have replaced the hacky `.remove()` cleanup logic for the `DropCursor` with a much safer CSS-based approach (`display: none` and `opacity: 0`). 

This prevents the `Uncaught TypeError: Cannot read properties of null (reading 'removeChild')` error because the DOM elements remain intact for ProseMirror's internal state management, while perfectly hiding the "ghost" lines visually during rapid dragging.

The TypeScript build (`npm run build`) completed successfully, and the dev server (`npm run dev`) is now running. You can test the drag-and-drop functionality in your local browser to confirm the ghost lines and console errors are completely resolved!
