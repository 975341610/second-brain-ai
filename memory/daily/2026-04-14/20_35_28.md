Agent: 老大查明原因啦！这次我抓到了 3 个导致点不动的隐蔽小 Bug：

1. **状态漏接**：`EditorHeader` 确实把切换动作传出来了，但是外层的 `NovaBlockEditor` 里接这个事件的回调函数居然是个空的（忘了写 `setNote`），所以前端直接当场没反应。
2. **保存漏传**：保存笔记的 `handleSave` 方法里，打包发给后端的数据里少塞了 `background_paper` 字段，导致哪怕状态变了，一刷新又变回白纸了。
3. **图层被盖**：我在画点阵和格子的 `BackgroundPaper` 里加了 `z-index: -1`，原本想让它垫在文字底下，结果被外层编辑器自己的纯白色大背景给完全闷在下面了，导致啥也看不见。

**现在的修复情况：**
小分队已经把上述 3 个点全部打通并改好了！把状态更新逻辑加上、保存接口加上了新字段，并且去掉了那个画蛇添足的 `z-index: -1`，确保纸张背景完美垫在文字层下方。

你现在刷新一下云端网页试试，点阵和格子肯定能一秒换肤了，而且刷新之后也会老老实实记住！如果有其他瑕疵随时喊我～✨

*(排查过程传送门：[Session 详情](https://code.byted.org/copilot/chat/a09a1a52-ca7e-4f70-82af-63ec66cea290))*
