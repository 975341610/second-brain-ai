Agent: Superpowers 已就位！方案已经出来了，非常硬核！🚀

### 🛠 v0.4.8 升级方案细节：
1.  **后台任务独立 Session**：在 `background_index_note` 函数内部直接开辟独立的数据库连接（`SessionLocal`），彻底解决“保存笔记后 AI 任务报错”的顽疾。
2.  **AIClient 连接池**：在类级别维护一个持久化的 `httpx.AsyncClient`，让 AI 请求告别频繁握手，响应快如闪电。⚡
3.  **流式重试机制**：给 `stream_chat` 加上最多 3 次的网络重试，并增强 SSE 解析的容错性，哪怕网络有点小波动，AI 也能稳稳输出。
4.  **版本号跃迁**：直接升级到 **`v0.4.8`**。

方案已确认，我现在就让子代理按照 Superpowers 的标准流程开始动工！请稍候，我会带回最终的“竣工报告”！🏗️🦸‍♀️✨
