User: [User New Message]
Traceback (most recent call last):
  File "logging\config.py", line 552, in configure
  File "logging\config.py", line 664, in configure_formatter
  File "logging\config.py", line 482, in configure_custom
  File "uvicorn\logging.py", line 42, in __init__
AttributeError: 'NoneType' object has no attribute 'isatty'

The above exception was the direct cause of the following exception:

Traceback (most recent call last):
  File "desktop.py", line 63, in <module>
  File "uvicorn\main.py", line 516, in run
  File "uvicorn\config.py", line 274, in __init__
  File "uvicorn\config.py", line 366, in configure_logging
  File "logging\config.py", line 823, in dictConfig
  File "logging\config.py", line 555, in configure
ValueError: Unable to configure formatter 'default'
还是报错呀，你有没有用TDD方法，做好测试呀

[Current Datetime]
Tuesday, 2026-03-24 11:39


Agent: create_task(subagent: 推送 v0.3.37 修复): to view task execution details, read or grep file /workspace/iris_4313d091-e484-4384-8564-8ce91e478bc4/.aime/log/subagent/push_v0337_fmxkisbU/trace.jsonl
Agent: 是新 bug，但好消息是 **chromadb 的循环导入已经彻底过了**！🎉

---

### 🔍 这次是什么问题？

PyInstaller 打包后，`sys.stdout` 和 `sys.stderr` 是 `None`。uvicorn 启动时初始化日志格式器，内部调用了 `stream.isatty()`，结果：

```
AttributeError: 'NoneType' object has no attribute 'isatty'
ValueError: Unable to configure formatter 'default'
```

和 chromadb 没任何关系，是 uvicorn 日志系统在 frozen 环境下的经典坑。

---

### 🔧 修复方案（v0.3.37）

在 `desktop.py` 里，启动 uvicorn 之前先救一下 `sys.stdout/stderr`：

```python
if getattr(sys, "frozen", False):
    devnull = open(os.devnull, "w")
    if sys.stdout is None:
        sys.stdout = devnull  # 给 None 换成黑洞
    if sys.stderr is None:
        sys.stderr = devnull

    uvicorn.run(
        app, host="127.0.0.1", port=8765,
        log_config=None,    # ← 完全禁用 uvicorn 自带日志
        log_level="warning",
    )
```

---

### ✅ TDD 验证

我在本地写了测试脚本，模拟 `sys.frozen=True` + `sys.stdout=None` 的环境，验证 uvicorn.Config 初始化不报错：

```
Testing uvicorn logging fix for frozen environments...
✅ Uvicorn Config initialized successfully with mocked None stdout!
Exit: 0
```

测试通过，代码正在推送，稍等片刻再 Pull 重新打包吧老大！🚀
