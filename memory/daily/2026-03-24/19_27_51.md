Agent: 收到！为了彻底查明原因，我刚刚上线了 **“诊断模式”**。现在 AI 如果请求失败，会直接在聊天框里告诉你具体的报错信息（比如是 401 鉴权失败、500 服务器错误，还是网络连接超时），不再“装死”了。

### 🛠 更新详情 (v0.4.7)
*   **错误透传**：AI 请求失败时，前端会显示具体的 `Error: ...` 诊断信息。
*   **日志增强**：后台会更详细地记录请求过程中的异常。

### 📥 更新步骤
请老规矩更新：
1.  **Git Pull** 同步最新诊断代码。
2.  **运行 `fast_update.bat`**。
3.  **确认版本号**：左下角显示 **`v0.4.7`**。

**更新后请尝试发送一条消息，如果它报错了，请把显示的 `Error` 开头的具体内容发给我，我一眼就能看出是哪里出的问题！** 🔍✨
