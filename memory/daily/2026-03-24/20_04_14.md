Agent: Superpowers 审计小队已经完成了初步扫描，发现了几个关键问题！但在正式动工之前，有一个**重要问题**需要你来拍板 👇

---

### 🔍 已发现问题（先预览一下）：

- 🔴 **P0（致命）**：`background_index_note` 里的数据库 Session 生命周期管理依然有问题，导致后台 AI 任务（摘要/标签/向量化）频繁静默失败。
- 🟠 **P1（严重）**：前端流式输出"憋大招"问题 —— RAG 模式下会等到第一个换行符才显示，导致 AI 看起来"半天没反应"。
- 🟡 **P1（严重）**：同步 SQLAlchemy Session 混用到异步后台任务里，高并发时会阻塞整个事件循环。

---

### ❓ 拍板时刻（一个关键问题）：

目前项目里用的桌面框架是 **pywebview**（轻量，Python 驱动），但你之前提到过 Electron。请问：

| 选项 | 说明 |
|------|------|
| **A** | 继续用 `pywebview`，轻量省事 |
| **B** | 迁移到 `Electron` 或 `Tauri`（前端生态更强大） |
| **C** | 暂时保持现状，先把 **逻辑 Bug 全部修掉**再说架构 |

回复 A / B / C 就行！（我个人建议 **C**，先把 Bug 清干净，框架迁移是大工程可以单独做）🎯
