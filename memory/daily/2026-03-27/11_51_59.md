Agent: 老大，代码小助手（Subagent）刚去研究了一下咱们切出来的 116 张素材，并且做了一份设计规划（Plan），现在跑来问一个架构上的问题。

小助手建议采用 **Approach A: Tailwind 扩展** 的方式，把素材配到 `tailwind.config.js` 里（比如 `p5-torn-divider`、`mask-p5-torn` 等），这样全局都能复用。

但是在改到底部导航条的时候，它发现目前的底部导航代码还写在 `App.tsx` 里。
**小助手想问：**
为了更好地应用 P5R 复杂的撕纸边缘（mask-image）效果和写单元测试，**需不需要把 `App.tsx` 里的底部导航条抽离出来，重构成一个独立的 `BottomNav.tsx` 组件？** 还是说先保持现状，直接在 `App.tsx` 里改就行？

你怎么看？（抽离出来会更规范，但也稍微花点时间）
