Agent: 当前还是没生效：我这边验证到 **`/api/version` 仍然没有返回版本信息**，而且前端里 **SplashScreen 仍写死 `Build v0.5.4`**，所以你看到 5.4 是对的。

我继续把这两处修到位（补齐后端 `/api/version` + 改掉前端写死文案），修完我会先本地 `curl /api/version` 确认返回 `v0.5.5` 再让你刷新。
Agent: create_task(traecli: 重试：补齐/api/version并修正前端写死版本，完成curl验证闭环)
