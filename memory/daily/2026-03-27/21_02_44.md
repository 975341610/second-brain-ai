Agent: 收到，开始桌面端。

我刚对齐了方案与飞书上下文：桌面端走 **混合路线（Hybrid）**——编辑器内容用 `Tiptap + Yjs + y-indexeddb`（离线/CRDT），元数据/附件/向量搜索继续复用 **本地 Python 后端（FastAPI + SQLite + Chroma）**，由 Electron 主进程负责拉起/管理后端进程。

开工前我只需要你拍板 3 个点（避免后面返工）：
1) Electron 脚手架：用 **electron-vite** 还是 **electron-forge**？
2) 打包方案：优先 **electron-builder**（出安装包/便携包）可以吗？
3) MVP 里程碑：先做“**Electron 壳 + 启动后端 + 渲染加载现有前端**”，还是先做“**离线编辑器原型（Yjs 持久化）**”？

方案原文我已经拉下来了：
- Second_Brain_AI_离线优先客户端技术方案.lark_1.md
