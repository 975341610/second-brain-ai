User: [User New Message]
刷新后还是5.4

[Current Datetime]
Friday, 2026-03-27 11:00


Agent: 收到，说明不是你没刷新，是**页面里显示版本号的位置还写死/读错了**。我现在直接去源码里把 `5.4` 的来源揪出来，不再猜。揪出来后我会重新构建并验证到页面文案为止。
Agent: 破案了：你看到的 5.4 不是缓存，是前端在启动时会调用后端 `/system/version`，而后端现在仍然返回 `v0.5.4`，于是把页面左下角版本号覆盖回去了；另外 SplashScreen 里也写死了 `Build v0.5.4`。

我现在把这两处一起改：
1) 后端 `/system/version` 返回更新后的版本（v0.5.5）；
2) 前端所有写死的 v0.5.4 文案统一改为从同一处读取。
改完我会本地 curl 验证 `/system/version` 真变成 v0.5.5，再让你刷新确认。
Agent: create_task(traecli: 修复版本号始终显示0.5.4：统一后端/system/version与前端展示)
