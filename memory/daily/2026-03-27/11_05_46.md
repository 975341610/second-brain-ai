Agent: 你说得对：现在确实还没更新成功。我刚定位到根因：前端读取版本/初始化走的接口不是 `/system/version`，而是我们之前白名单里的 **`/api/version`**（你看我这边 `curl /api/version` 还是 404）。所以前端拿不到新版本/主题配置，设置页当然也不会出现“主题”下拉框。

我现在去把后端版本接口补齐/修正为 `/api/version`，并把 SplashScreen 里写死的 `Build v0.5.4` 改成动态；然后重新 build + 重启后端。等我这边验证 `curl /api/version` 能返回 `v0.5.5` 再让你刷新。
Agent: create_task(traecli: 修复/api/version缺失导致版本不更新与主题入口不显示)
