User: [User New Message]
现在这个样子是符合预期的吗？

---

[Mentions]
Here are some stuffs mentioned by the user, and they are already preprocessed to the workspace. You don't need to download them again.
[img_v3_02106_4fbcb14d-5373-47d4-9dc3-59988319138g.png]: attachment downloaded to: ./img_v3_02106_4fbcb14d-5373-47d4-9dc3-59988319138g.png
Note: Some stuffs might not be automatically recognized and processed, in this case you need to download them manually.
If the user requests that you should read any of these items, must read them first before proceeding.


[Current Datetime]
Friday, 2026-03-27 14:20


Agent: 哈哈哈哈！看到截图我都要笑出声了！这**绝对不符合预期**！这完全是 CSS 层级冲突导致的“群魔乱舞”！💥

我来分析一下这张截图里的“四大名场面”：
1. **行内代码成了血红色的粗块**：这说明 `VLOOK` 主题里写的红色行内代码样式泄漏到了 `Cyberpunk` 主题里，因为权重太高，或者没有被正确隔离。
2. **代码块背景成了刺眼的粉白网格**：这是致命错误！`pre code` 的背景应该是深邃的暗黑色，结果它继承了可能属于别的地方的浅色背景，而且还把 `Cyberpunk` 的网格纹理给透出来了，这根本没法看代码。
3. **字号小得像蚂蚁**：代码块的字体大小（`font-size`）和行高（`line-height`）被压缩了，失去了原本等宽字体（Monospace）那种工整的排版感。
4. **边框挤成一团**：原本应该有的内边距（padding）和圆角都没了，内容死死地贴在边框上。

**这说明什么？**
说明刚才强行引入的 `atom-one-dark.css` 虽然带入了颜色，但**整个代码块容器（`<pre><code>`）的基础底座被外围的各种流氓 CSS 给彻底摧毁了！**

**【终极抢救指令】**
我现在必须亲自安排一次极其精确的“重型装甲包覆”。我要给 `[data-theme="cyberpunk"] .ProseMirror pre` 和 `code` 强制穿上防弹衣：
1. **强制深色背景**：死死锁住深黑色 `background: #1e1e24 !important`，绝不透出网格。
2. **强制排版比例**：加上 `padding: 1rem !important`，调大 `font-size` 和行高，让字距恢复舒适感。
3. **彻底隔离行内代码和代码块**：明确区分单反引号 \`行内代码\` 和三反引号 ```代码块```，绝不让行内代码的红色方块污染大段的代码区。

我立刻去手撕这几条 CSS！半分钟就好！🚀
Agent: create_task(traecli: 终极抢救赛博朋克代码块渲染崩溃)
