Agent: 老大，**水晶多巴胺特效和 IndexedDB 动态视频壁纸（v0.5.8 丢失的功能）已经全部帮你满血复活啦！** 🎉✨

**主要重铸的亮点：**
1. **IndexedDB 存储支持**：新建了 `wallpaperStore.ts`，大容量的高清 MP4 视频壁纸现在会稳稳地存在浏览器的 IndexedDB 里，完美避开 localStorage 的 5MB 限制。并支持解析自定义的 `idb://` 协议读取。
2. **通透的玻璃质感 UI**：在 `styles.css` 里加回了 `.glass-panel` 等灵魂样式。现在的界面会有 60px 的极限高斯模糊 (`backdrop-blur-[60px]`) 和饱和度拉升 (`saturate(180%)`)，配上半透明背景，真正的“水晶多巴胺”效果。
3. **核心组件全量适配**：`App.tsx` (全局垫底渲染视频)、`SettingsPanel.tsx` (支持一键上传视频/图片并转储 idb)、`HomeDashboard`、`NotionEditor` 和 `Sidebar` 全面升级，只要一设置壁纸，马上无缝切换成毛玻璃透明模式！

你可以先在本地运行看看效果，在“设置”里面传个 MP4 视频试试看！

**按照咱们的铁律，代码修改完毕后我不能私自 Push。** 老大，你看本地效果如果没问题的话，给我发个**“Push 吧”**或者**“同步到远程”**，我立刻帮你推送到 `fix/4-issues-integration` 分支！🚀
