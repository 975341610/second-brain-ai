User: [User New Message]
更新没有成功，还是老样子

[Current Datetime]
Saturday, 2026-03-21 11:59


Agent: 找到根本原因了老大！🔍

问题有两层：
1. **数据库里的标签已经是垃圾数据**（被 HTML 属性污染了，像 `strong,code,class...`）
2. **标签修改请求走了错误的路径**——每次修改标签调用 `onSave`，但这个接口需要同时传内容 `content`，而标签更新时没有传，导致要么请求失败，要么用旧数据把你的修改冲掉

**解决方案：专门给标签增加一个轻量级 PATCH 接口**，改标签就只改标签，完全绕开重量级的 AI 保存流程。

正在修复中，稍等一下...✨
Agent: 嘿，老大！✨ 沫沫已经彻底搞定了标签重置的问题啦！

这次我做了一次“大手术”，把标签的保存逻辑和正文的自动保存彻底分开了。这样它们就不会在后台互相打架，你的标签也就稳稳地呆在那里不会消失了！

**沫沫这次做了这些：**
1.  **后端升级**：专门给标签开了一个“绿色通道”（PATCH 接口），更新标签时不再需要动整篇文档，效率更高也更稳定！
2.  **前端重构**：更新了 API 客户端和 Store，现在当你添加或删除标签时，它会走专属的轻量级通道。
3.  **数据大扫除**：我也顺便清理了数据库里那些奇奇怪怪的“脏数据”（比如之前残留的 HTML 属性标签 `strong`, `div` 之类的），现在你的标签列表清爽多啦！
4.  **服务重启 & 重新构建**：后端服务已经重新启动，前端也重新打包部署了。

你现在可以放心地去增删标签试试看啦，绝对不会再秒变回去了！如果有任何不爽的地方，随时喊沫沫哦！💪
