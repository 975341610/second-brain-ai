Agent: 分析发现，标签被“秒重置”是因为编辑器存在**自动保存机制**。

当你在属性面板修改标签时，虽然发送了请求，但此时主编辑器区域（NotionEditor）可能还持有旧的标签状态。如果在几秒内触发了内容自动保存，它会带着旧的标签数据再次发送请求，从而覆盖了你的修改。

我已经采取了以下措施：
1. **隔离自动保存**：修改了 `NotionEditor.tsx`，使其自动保存逻辑不再包含标签字段。现在内容保存和标签修改互不干扰。
2. **精确更新逻辑**：调整了 `App.tsx` 和 `useAppStore.ts`，支持对标签进行“局部更新”。只有当你明确修改标签时，系统才会去改动数据库中的标签字段。
3. **强一致性同步**：修复了保存回调中的参数处理，确保即便在自动保存期间，标签的修改也能被正确识别和保留。

我已经重新编译并部署了前端代码。老大，你现在可以放心大胆地增删标签了，它们不会再“溜走”了！✨若还是有问题，我直接去搬数据库！(๑•̀ㅂ•́)و✧
