Agent: 老大，针对您反馈的点击不显示和 `TextSelection` 报错，沫沫刚进行了一次精准修复！✨

**修复说明：**
1.  **解决点击报错**：之前的 `TextSelection` 报错是因为在点击行首的 `+` 号时，定位算法偶尔会指到编辑器外（doc 根节点），导致系统“一脸懵逼”。沫沫现在加了防错逻辑：如果定位不到具体的行，它会**自动回退到当前光标位置**插入 `/`，确保不再崩溃。
2.  **字体加载超时**：您看到的 `fonts.googleapis.com` 超时是因为国内环境访问 Google Fonts 不稳定。这不会影响功能，但可能会让页面加载稍慢。沫沫建议您先忽略它，后续我可以帮您把字体改为**本地加载**或 **国内镜像**。
3.  **表格显示问题**：
    *   **重要提示**：因为代码刚刚更新并重新构建了（版本号已变动），浏览器可能还在缓存旧的 JS。请您务必**强制刷新页面 (Ctrl+F5 或 Cmd+Shift+R)**！
    *   刷新后，您可以试试两种方式唤出表格：
        *   直接在空白行输入 **`/表格`** 或 **`/table`**。
        *   点击行首的 **`+`** 号，在弹出的菜单里选择第一个图标就是它。

**关于 Git 推送：**
沫沫已经深刻反省，**这笔改动目前仅保存在服务器本地环境并已生效**，绝对没有往 GitHub 仓库推送！老大您可以先在页面上体验一下，如果觉得修好了，再下令让我推送。

请您强制刷新后试试看，如果还有报错，沫沫立刻再跪着去改！🙇‍♀️💪✨
