User: [User New Message]
我想将打卡日历的外观改成如图中这样的手绘风格，然后打卡效果改为每点一次日期格子就绘制一点下图中的某一个表情包，完成打卡就全部在日期格子中完整显示出表情包

[Current Datetime]
Tuesday, 2026-04-07 17:39


Agent: create_task(subagent: Convert Habit Tracker to Hand-drawn Style with Progressive Emoji Reveal): to view task execution details, read or grep file /workspace/iris_4313d091-e484-4384-8564-8ce91e478bc4/.aime/log/subagent/habit_handdrawn_AGGkBUxP/trace.jsonl
User: [User New Message]
在 `nova_repo` 目录下，重构 `nova-block/src/components/widgets/HabitTrackerComponent.tsx`，将现有的“高级磨砂台历”完全转换为**“手绘/手账风格 (Hand-drawn / Neo-brutalist)”**，并实现**表情包渐进式绘制打卡**效果。

### 详细 UI/UX 设计要求：

**1. 整体手绘风格 (Hand-drawn Style)**
*   **容器背景与边框**：移除原有的毛玻璃 `backdrop-blur` 和平滑阴影。改为类似真实纸张的背景色（如 `#fcf9f2` 暖白/米黄色），粗犷的深色边框（`border-2 border-stone-800`），以及硬阴影（手绘风典型的 `shadow-[4px_4px_0px_0px_rgba(28,25,23,1)]` 或类似效果）。
*   **字体**：全局或组件内指定手绘感字体，例如 `'Comic Sans MS', 'Chalkboard SE', 'Caveat', cursive`。
*   **不规则圆角**：利用 CSS 的特殊 `border-radius`（例如 `border-radius: 255px 15px 225px 15px/15px 225px 15px 255px`）让输入框、按钮、甚至整体卡片看起来像手画的不规则矩形。

**2. 表情包“渐进式绘制”打卡效果**
*   原先的打卡是背景颜色加深。现在改为**图案填充**。将习惯的 `icon`（即表情包）作为核心视觉元素放在日历格子里。
*   **未打卡状态**：格子里显示浅色、灰度的表情包轮廓（例如 `opacity: 0.1, filter: grayscale(100%)`）。
*   **渐进打卡交互**：每次点击，表情包从底部往上“画”出来（或者叫显形）。
    *   实现方案：在底层灰阶表情包的上方，绝对定位一个正常全彩、100% 透明度的相同表情包。
    *   利用 CSS `clip-path: inset(calc(100% - ${percent * 100}%) 0 0 0)`。
    *   例如：目标是 4 次。点第 1 次，`percent` = 0.25，`inset(75% 0 0 0)`，表情包露出底部 25%。点满 4 次，`percent` = 1，露出 100%。
*   **满级视觉**：彻底点满时，格子背景可以有一个轻微的高亮（比如蜡笔涂抹的淡黄色），表情包不仅完整显现，还可以加个轻微的 `framer-motion` 弹跳动效。日期数字退居其次（比如缩小放到左上角或右上角）。

**3. 保留之前的核心逻辑**
*   左键增加（封顶），右键减少（防误触）。
*   保留右下角的微型数字进度（如 `3/8`），可以用手写体。
*   保留满级时的 `canvas-confetti` 纸屑特效。
*   顶部切换习惯的下拉菜单、连胜天数（火焰/皇冠）、设置面板均保留，但 UI 统一改为带粗边框+硬阴影的手绘风。

请在 `nova_repo` 内修改对应的 `HabitTrackerComponent.tsx`。修改完成后，使用中文提交 commit `feat(habit): 将打卡日历改造为手账手绘风格与表情包渐进显形特效` 并 push 到 `main` 分支。

[Current Datetime]
Tuesday, 2026-04-07 17:39


Agent: 老大，你的品味绝了！从“高级冷淡风”秒切“治愈系手绘风”，这正是个人知识库最需要的**温度感**和**养成感**！

虽然我没法直接看到你发的图片，但我完全能脑补出那种可爱的**手绘手账风 (Hand-drawn / Sketchy style)**！

我刚刚已经立刻安排特工去对 `HabitTrackerComponent.tsx` 进行“画风突变”级别的手术了！以下是这次脑暴的落地实现方案：

### 🎨 1. 画风突变：手绘质感注入
- **抛弃玻璃，拥抱纸张**：去掉之前的磨砂玻璃和淡雅阴影，改成**米黄色纸张底色** (`#fcf9f2`)、**粗糙的深色描边** (`border-stone-800`) 和**硬边黑阴影**（就像真实的贴纸一样）。
- **随性笔触**：所有的输入框、按钮和卡片边缘，都不再是死板的圆角，而是通过特殊的 CSS 变成略微扭曲的“手绘不规则圆角”，字体也会换成类似粉笔/手写体 (`Comic Sans MS` / `Chalkboard SE`)。

### 🧩 2. 表情包“渐进式显形”打卡 (核心魔法)
- **底层打底**：格子里的日期数字会缩小放到左上角，把最核心的 C 位留给你的“表情包”（也就是习惯的 Icon，比如 💧、🏋️‍♂️、🍎 或者任何 Emoji）。没打卡时，表情包是一层极淡的灰色轮廓，就像还没涂色的线稿。
- **神奇的涂色魔法**：假设目标是点 4 次。当你点第 1 下，系统会用 CSS 魔法 (`clip-path: inset()`) 从下往上“揭开” 25% 的全彩表情包；点第 2 下，露出 50%……**每一次点击，表情包就像被马克笔从下往上涂了一层颜色！**
- **满血复活**：当你点满最后一下，整个全彩表情包彻底“破茧而出”，伴随着一个 Q 弹的动画和漫天飞舞的纸屑特效 🎉！旁边依旧保留手写体的进度提示（比如 `3/4`）。

### 💡 3. 保留硬核防误触
- 依然保留你最爱的：**左键涂色（增加），右键橡皮擦（撤销/减少）**。

特工正在全速重写 UI 样式和 CSS 裁剪动画，这个渐进式的涂色效果写出来绝对惊艳！稍等片刻，完成推送后立刻向你汇报！✨
