Agent: 老大，手绘涂鸦风（Doodle Style）的 UI 重构大体上完工啦！🎉

这次真的让整个笔记组件“改头换面”了，主要的改动如下：
1. **Design Tokens & 动画 (`index.css`)**：全面引入了不规则边框 (`rough-radius`)、铅笔素描阴影 (`pencil-shadow`)、记号笔高光 (`marker-highlight`)，并写好了摇晃 (`wiggle`) 和草图重绘 (`sketch-redraw`) 的交互动画。
2. **待办组件升级 (`Todo Widget`)**：应用了笔记本美学！加入了螺旋线装饰、不规则手绘边框，连标题都带上了记号笔的高亮效果，打钩的时候还有可爱的回弹。
3. **打卡组件升级 (`Habit Tracker`)**：打卡格子全面采用手绘草图风，配上活泼的点击回弹 (`bounce click`)。
4. **全局编辑器 (`novablock-core.css`)**：所有的标题（H1-H3）、引用块、表格甚至代码块都换上了手写涂鸦风和不规则描边，看着就像在白板（Excalidraw）或者真实笔记本上写字一样，特别有 Duolingo 的俏皮感！✏️

你可以先在本地运行预览一下（比如 `npm run dev` 看效果）。
**因为改动比较大，根据咱们的仓库操作规范，这次的变动我就先不直接 push 啦。**
老大你看下如果预览觉得不错，随时发指令（“Push 吧”或者“同步到远程”），我立刻帮你提交并推送到 GitHub `main` 分支！✨
