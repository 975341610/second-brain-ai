User: [User New Message]
我在哪儿设置日历的背景？

[Current Datetime]
Tuesday, 2026-04-07 18:23


Agent: create_task(subagent: Implement Calendar Background Upload): to view task execution details, read or grep file /workspace/iris_4313d091-e484-4384-8564-8ce91e478bc4/.aime/log/subagent/habit_bg_upload_VJJFaeFx/trace.jsonl
User: [User New Message]
在 `nova_repo` 目录下修改 `HabitTrackerComponent.tsx` 和 `HabitContext.tsx`，添加并实现“自定义日历背景壁纸”功能。

具体要求：
1. **类型扩展**：在 `HabitContext.tsx` 的 `Habit` 接口中增加 `bgImage?: string`，用于保存每个习惯独有的日历背景壁纸图片（Base64 或 URL）。
2. **设置界面**：在 `HabitTrackerComponent.tsx` 中 `isSettingOpen` 的每个习惯编辑栏（与刚才新增的 Camera 图标并排），增加一个 `<input type="file" accept="image/*" />` 对应一个专门的“换壁纸”按钮（如图标使用 `Image` 或 `Wallpaper`）。点击选择图片后，将其转为 Base64，通过 `updateHabit(h.id, { bgImage: base64 })` 保存。同样建议进行一定的画布压缩。
3. **渲染壁纸**：当用户在主界面选择了一个含有 `bgImage` 的习惯时，将这张图片作为打卡日历区域（格子的底层）的背景图片。
- 给装载格子的父容器添加对应的内联样式：`backgroundImage: activeHabit?.bgImage ? \`url(\${activeHabit.bgImage})\` : undefined`，并配合 `backgroundSize: 'cover'`, `backgroundPosition: 'center'`, `opacity: 0.25` 等。或者，如果想要不影响文本，可以使用一个绝对定位的 `div` 置于底层，显示这个 `bgImage` 并设置较低的透明度，比如 `opacity-20`。

注意：保留现有的粗犷手账风格。
完成后，提交名为 `feat(habit): 增加每个习惯专属的自定义日历背景壁纸上传与展示功能` 的 commit 并推送到 `main` 分支。如果因为网络失败可以尝试重试。

[Current Datetime]
Tuesday, 2026-04-07 18:24


Agent: 老大，太不好意思了，这是我的锅！🙈

刚才我牛皮吹早了，特工上个版本只给你做了**“上传自定义图标（贴纸）”**的功能，那个相机按钮 📷 只能换打卡的表情包，**把“换壁纸”的功能给漏掉了**！

我刚刚已经原地把特工拎回去重做了！现在正在紧急加上专属的**“换壁纸”按钮（就在刚才那个图标相机的旁边）**。
上传完壁纸后，它会垫在日历的最底层，自带一点点半透明效果，既有氛围感又不会挡住打卡的记录。

这会儿正在加急改代码推上去，稍微等我半分钟，跑完我马上通知你拉取最新代码！🏃‍♂️💨
