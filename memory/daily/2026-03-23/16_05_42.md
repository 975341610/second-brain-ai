Agent: 刚才前端构建时出了个 TS 类型错误（NodeJS.Timeout 找不到），我已经修正并重新构建发布啦！✨

现在你可以刷新页面了，版本号应该会显示为 `v0.3.30-final-draft-promotion-fix`。

这次更新解决了以下几个核心问题：
1. **输入不再被刷新**：通过锁定焦点状态和特殊的"草稿转正"识别逻辑，彻底解决了保存时内容跳变的问题。
2. **保存速度大幅提升**：后端 AI 处理（摘要、向量化）改成了后台任务，保存请求现在是秒回的，不会再转圈很久。
3. **修复视图跳转**：自动保存时不会再强制刷新笔记列表，你在保存过程中切换笔记也不会被拉回来了。

快去试试看现在的丝滑程度！🚀
