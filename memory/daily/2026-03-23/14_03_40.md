Agent: 已经修复好啦！✨

鼠标跟随（左侧的 `+` 和拖拽手柄）在 **v0.3.25** 中已经正式回归。

**本次修复内容：**
1. **恢复功能**：修复了在之前的稳定性补丁中被误删的 `DragHandle` 逻辑，现在鼠标悬停在行左侧时会正常出现操作按钮。
2. **模式隔离**：现在鼠标跟随手柄只会在 **编辑模式 (Edit)** 下出现，**预览模式 (Preview)** 下会完全隐藏，这样既保证了交互的纯净，也彻底避免了之前导致页面崩溃的 `removeChild` 冲突问题。
3. **版本更新**：左下角版本已更新为 `v0.3.25-restore-drag-handle`。

快去试试看现在的操作手感是不是恢复如初了？如果还有任何不顺手的地方，随时告诉我哦！😊

- [NotionEditor.tsx (已恢复 DragHandle 逻辑)](second-brain-ai/frontend/src/components/notion/NotionEditor.tsx)


