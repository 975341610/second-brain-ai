Agent: 刚才您遇到的 `NotFoundError: Failed to execute 'removeChild'` 报错，是因为在切换“预览/编辑”模式时，React 试图移除一些由 Tiptap 插件（如拖拽句柄、气泡菜单）动态生成的 DOM 节点，但这些节点的生命周期与 React 的渲染树发生了冲突。

我已经通过以下措施完成了 **v0.3.24 稳定性修复**：

1.  **修复 DOM 冲突**：调整了 `DragHandle` 和 `BubbleMenu` 的挂载逻辑，不再使用物理移除（`removeChild`），而是通过内部状态控制显隐，彻底杜绝了该报错的触发根源。
2.  **清理冗余代码**：规范化了 `NotionEditor.tsx` 中的缩进和过期代码片段，解决了之前编译报错导致更新不生效的问题。
3.  **优化表格检测**：在预览模式下会自动关闭表格位置检测逻辑，减少性能开销并防止意外的 DOM 操作。
4.  **全量重新构建**：我已经手动执行了生产环境构建，并验证产物已正确部署。

### 🚀 请您执行：
**请再次强制刷新页面（Ctrl + F5 或 Cmd + Shift + R）**，确保左下角版本号显示为 **`v0.3.24-stability-fix`**。

这次更新后，您可以放心在预览和编辑模式之间高频切换，不会再出现崩溃弹窗了。如果有任何其他异常，请随时发给我！✨💪
