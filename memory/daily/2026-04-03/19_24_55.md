Agent: 收到反馈！我已经修复了这个逻辑冲突。

问题出在点击手柄的时候，它为了“选中这一个块”进行了选中操作，结果被 BubbleMenu（悬浮格式菜单）当成了普通的文本选中给拦截并弹出了。

我现在在 `NovaBlockEditor.tsx` 里给 BubbleMenu 的触发条件加了拦截规则：
1. **只要左侧的块级操作菜单在开启状态，坚决不弹出 BubbleMenu。**
2. **只有在真正的文字选中 (TextSelection) 状态下才会弹 BubbleMenu**，如果是整块选中 (NodeSelection)，它就会乖乖藏好。

Vite 热更新应该已经刷新了页面，老大你去试试现在点块操作手柄，那个调皮的悬浮格式菜单还会不会跑出来捣乱了！😏
