Agent: 啊哈哈，果然是被 Tailwind Typography（`prose`）的“流氓排版规则”给干掉了。

因为 `prose` 默认会把所有的列表重置、加上自己的圆点（`::marker` / `::before`伪元素），并且覆盖掉表单元素的默认外观。

我已经针对性地在 `index.css` 里给咱们的 `taskList` 和复选框打了强心针（`!important` 以及恢复 `appearance: auto`），并把那些多余的圆点隐藏掉了。

老大你现在刷新页面，复选框应该已经乖乖带着它的蓝色高亮回来啦，你再体验看看有没有其他被 `prose` “误伤”的样式！🛡️
