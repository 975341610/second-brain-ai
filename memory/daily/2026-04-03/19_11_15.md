Agent: 啊，原来是导入类型和 API 差异引起的。

刚才的报错是因为 `ChainedCommands` 是 `@tiptap/core` 里的类型，而之前误从 `@tiptap/react` 导入了，并且 `tippyOptions` 在你当前版本的 `DragHandle` 扩展里是不支持的属性。

我已经修复了这些类型和 API 不兼容的错误，重新 `npm run build` 也 100% 编译通过了。Vite 应该已经热更新了，老大你再直接刷新页面看看，现在应该一切恢复正常，刚才做的高级菜单和手柄功能也都能爽爽地用啦！
