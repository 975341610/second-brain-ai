Agent: 修复了！

之前因为我们这个是完全重构的纯净版 `nova-block` 工程，丢失了 Tiptap 原版的一些内置样式支持（比如没装 `@tailwindcss/typography` 插件来解析 `prose`），导致很多块元素只是个裸标签。

我已经直接在全局 CSS 中给 **引用块 (blockquote)** 和 **待办事项 (taskList/taskItem)** 手动加上了精细的样式。

现在：
- **引用块**：左侧会有灰色的引用线，文字自动变为优雅的斜体和灰色。
- **待办事项**：去掉了难看的默认小圆点，正确渲染了可点击的蓝色复选框（Checkbox），并且和文字高度自动对齐。

热更新完毕，老大你刷新再测试看看显示效果怎么样！🌟
