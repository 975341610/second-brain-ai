User: [User New Message]
1.AI没有流式输出
2.AI有时候不回答/无反应
3.完全没有针对我们的笔记应用做适配和优化，根本回答不了关于笔记的问题

---

<system_notification type="skill_update">
Skills Removed:
- **deepwiki**
- **meego-platform-guidelines**

</system_notification>

[Current Datetime]
Saturday, 2026-04-11 23:28


User: [User New Message]
AI压根儿就不会根据笔记的上下文来回答/处理问题

---

<system_notification type="skill_update">
Skills Removed:
- **meego-platform-guidelines**
- **deepwiki**

</system_notification>

[Current Datetime]
Saturday, 2026-04-11 23:30


Agent: 收到！我已经定位到这三个问题的原因：

1. **没有流式输出 & 偶尔无响应**：因为本地模型推理（`llama-cpp-python`）是阻塞操作，当前代码在异步事件循环中直接执行了 `for chunk in response:`，导致整个后端的事件循环被死锁阻塞了，数据无法像流水一样发给前端，甚至会引发超时导致无反应。
2. **缺乏上下文**：目前前端调用 `streamInlineAI` 时，`context` 被硬编码传了空字符串 `''`，导致 AI 完全不知道你当前笔记写了什么内容。
3. **缺乏笔记应用优化**：后端的 system prompt 没有强调“个人知识库/笔记助手”的设定。

我马上指派代码助手（Traecli）对这三个点进行针对性修复：
1. 后端引入 `asyncio.to_thread` 解除流式输出的事件循环阻塞，恢复真·流式打字效果。
2. 前端把 `editor.getText()` 作为上下文塞给 AI。
3. 后端重写更专业的系统提示词（System Prompt）。

任务已创建，稍等片刻！
Agent: create_task(traecli: Fix Local AI Streaming and Context)
