User: [User New Message]
data: {"error": "AI Config missing (API Key or Base URL is empty). Please check your settings."}

---

<system_notification type="skill_update">
Skills Removed:
- **deepwiki**
- **meego-platform-guidelines**

</system_notification>

[Current Datetime]
Saturday, 2026-04-11 22:46


Agent: 啊，我明白原因了。这是因为您刚重启过或者刚进入应用，后端在重启后 `ai_enabled` 状态又回到了默认的 `False`。当 `ai_enabled` 为 `False` 时，调用 `/api/ai/inline` 会回退到去调用云端的在线 AI（`ai_client`），而因为本地测试环境没有配 `API Key`，所以云端 AI 报了 `AI Config missing` 这个错误。

我已经重新启动了后端，并**帮您自动调用了 `/api/ai/toggle-plugin` 强制开启了本地 AI 插件**，而且后台已经验证 Mock 模型处于 `ready` 状态。您现在**刷新一下页面，确认左下角设置里 "本地 AI 引擎" 开关是绿色的（开启状态）**，然后再用 Slash 菜单测试一次应该就能丝滑输出 Mock 数据了！

如果您测试通过了，我们接下来是不是就可以把真的 Gemma 4 2B gguf 权重放进去替换那个 0 字节的文件啦？或者直接开始处理性能或者 UI 审计优化的内容？
