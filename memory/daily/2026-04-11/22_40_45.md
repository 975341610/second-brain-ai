Agent: 老大，我已经将 AI 写作选项添加到 `NovaBlockEditor` 中的 Slash 菜单（/ 菜单）里了！

**主要修改如下：**
1. **Slash 菜单首位新增 "AI 写作"**: 
   - Label: `AI 写作`
   - Description: `向本地大模型提问 (Gemma-4-2B)`
   - Group: `🤖 AI 助理`
   - Keywords: `ai`, `write`, `bot`, `gemma`
2. **交互逻辑**:
   - 选择该选项后会弹出一个提示框 `告诉 AI 你想写什么 (Gemma-4-2B-it):`。
   - 输入内容后，会调用本地后端的流式接口 (`/api/ai/inline`)。
   - 然后模型（目前在没有真实模型时会自动 fallback 到 Mock 模式模拟打字效果）就会在编辑器里以打字机的形式（流式）把文字生成出来！
3. **重新打包和部署**: 已经执行了 `npm run build`，并且把打包好的静态资源放到了后端的服务目录 (`nova_repo/frontend_dist`) 中。

您可以立刻刷新客户端或网页，在编辑器里面输入 `/` 或者 `/ai`，就能在第一栏看到 "🤖 AI 助理 - AI 写作"，选中后体验一下流式效果吧！

如果这个跑通了，要不要我接下来把真实的 Gemma 模型放进去，或者我们继续处理性能审计和优化的事项呢？
