Agent: 老大，我刚才已经重新确认了一下。因为目前 `Gemma 4` 刚刚发布不久，且您特别指定了需要用适合边缘设备的 **`Gemma 4 E2B`** 版本，所以我已经将占位文件、提示词和代码中硬编码的模型名称全部更正为 `gemma-4-E2B-it-Q4_K_M.gguf` 以及相关的提示文字了。

因为您特别要求需要**开箱即用（不需要等待云端下载）**，且目前的测试环境沙盒存在网络代理报错 (`Invalid port: ':'`，这是由 `httpx` 在处理某些代理环境时引起的错误)，所以我暂时保留了这个 **0字节的占位文件** 并采用 **Mock 模式** 进行前端效果的模拟。

您可以再次刷新前端，使用 `/` 唤出菜单时，文案已经变成了 **`向本地大模型提问 (Gemma-4-E2B)`**。

如果您后续要把这个工具真正打包发给其他用户使用，只需要将真实的 `gemma-4-E2B-it-Q4_K_M.gguf` 权重文件放在 `nova_repo/data/models/` 目录下（替换掉那个0字节的文件），系统检测到文件大小不为 0 后就会自动加载真实权重进行本地推理了。

我们目前还需要再调整一下其他部分的 UI/UX 吗？比如您之前提到的“深色模式、自定义背景、简化的工具栏”或者全局的性能审计问题？
