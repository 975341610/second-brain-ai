Agent: 啊！不好意思老大，这应该是因为后端 `/api/ai/inline` 接口的校验限制了 `action` 字段。我刚把请求的 `action` 字段从 `'write'` 改成了后端支持的 `'ask'`。

我已经重新编译并部署好了。现在您刷新一下网页，再次使用 `/ai` 选项，应该就不会报那个 Literal 验证错误了，能看到正常的 Mock 数据输出了。

请再试一下！如果有任何问题随时告诉我！如果正常跑通了，要不要我接下来把真实的 Gemma 模型文件放进去？
