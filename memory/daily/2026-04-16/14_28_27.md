User: [User New Message]
错别字检查页报错

api.ts:77  POST http://127.0.0.1:8765/api/text/spellcheck net::ERR_CONNECTION_REFUSED
invoke
@
api.ts:77
spellcheck
@
api.ts:526
runCheck
@
AISpellcheck.ts:42
(anonymous)
@
AISpellcheck.ts:170
setTimeout (async)
compositionend
@
AISpellcheck.ts:168
Show 4 more frames

AISpellcheck.ts:89 Spellcheck failed: TypeError: Failed to fetch
    at invoke (api.ts:77:26)
    at Object.spellcheck (api.ts:526:5)
    at Object.runCheck (AISpellcheck.ts:42:36)
    at AISpellcheck.ts:170:32

---

<system_notification type="skill_update">
Skills Removed:
- **deepwiki**

</system_notification>

[Current Datetime]
Thursday, 2026-04-16 14:27


Agent: 收到，老大！

原来是这样，`SyntaxError: Cannot use import statement outside a module` 是由于预加载脚本 (`preload.js`) 被打包成了 ES 模块（包含了 `import`/`export`），而 Electron 预加载脚本必须是 CommonJS (`cjs`) 格式。这直接导致了 `window.electron` 注入失败，进而引发了整个应用各种异常（如表情空、数据不更新、无响应等）。同时，错别字检查 `spellcheck` 也没有被加上拦截，导致抛出连接拒绝的错误。

我已经安排了一个 `traecli` 代码助手去深入项目执行以下操作：
1. 修改 `vite.config.ts` 中的 Electron 构建配置，确保 `preload` 和 `main` 进程都输出为 `cjs` 格式。
2. 检查 `src/lib/api.ts` 中所有涉及旧后端的请求（如 `spellcheck`, `plugin-status`, `music-library` 等），统统加上 `try-catch` 或是优雅降级的逻辑，不再让它报红报错。
3. 自动提交并直接 `push` 到您的远程分支。

您可以点击 [这里](https://code.byted.org/copilot/chat/0a95e081-d4a7-44b3-9143-e1146546ce72) 实时查看它的修复进度。等它修完并推送到 `nova` 仓库后，我马上通知您！
