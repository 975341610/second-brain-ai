User: [User New Message]
我只有githubdesktop，没有git

---

<system_notification type="skill_update">
Skills Removed:
- **deepwiki**

</system_notification>

[Current Datetime]
Thursday, 2026-04-16 13:21


Agent: 啊！懂了老大！用 GitHub Desktop 完全没问题，图形化界面点起来更省事。

您按下面这两步在 **GitHub Desktop** 里点一下就行：

### 第一步：在 GitHub Desktop 切换分支
1. 点一下软件右上角的 **"Fetch origin"**（获取/刷新远程最新状态）。
2. 点一下正上方的 **"Current Branch"**（当前分支）下拉菜单。
3. 在搜索框里输入 `phase4-updated`，下面就会刷出来 `feature/novablock-phase4-updated`。
4. 点它！就成功切换过去了！

### 第二步：清理缓存并启动（在您平时敲 npm 的那个终端里）
虽然 Git 用的是客户端，但启动项目咱们还是得在终端（比如 VSCode 的终端、或者 CMD 里）敲命令：
1. 打开您的终端，进入 `nova-block` 目录：
   ```cmd
   cd C:\AI\nova\nova-block
   ```
2. 直接执行咱们的暴力清空三板斧（复制粘贴按回车）：
   ```cmd
   rmdir /S /Q node_modules
   del package-lock.json
   npm install
   ```
3. 等进度条跑完，敲启动命令：
   ```cmd
   npm run dev
   ```

如果您刚才已经保存了我写的那个 `start_nova_phase4.bat` 脚本，在 GitHub Desktop 里切完分支后，直接双击运行那个脚本选 `y` 也是一样的效果！快去点点看！
