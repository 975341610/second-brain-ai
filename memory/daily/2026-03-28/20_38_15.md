Agent: 我已经更新了 `backend/main.py`，确保后端在启动时能正确读取由 Electron sidecar 传入的 `PORT` 和 `HOST` 环境变量。代码已经提交到了 `fix/4-issues-integration` 分支，但推送时遇到了权限问题，您可能需要手动 push 一下，或者确认我的环境凭证。

接下来，我将继续处理 `src/main/sidecar.ts`，为其添加可靠的日志记录，以便捕获打包后 `backend.exe` 的标准输出和错误信息，从而彻底排查 Windows 下启动卡死的问题。
Agent: create_task(traecli: Add logging to sidecar.ts)
