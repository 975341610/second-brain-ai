Agent: create_task(traecli: Commit tray icon fix and error handlers)
Agent: 看日志，子进程（`backend.exe`）这回成功启动了！它不再报路径找不到的错误，而且能够获取到正确的目录。

但是如果在启动后，**界面依然卡死或者闪退不显示**，这里通常有一个深坑：**图标找不到导致的进程崩溃** 💥。
Electron 在打包时如果没有配置好，`resources/icon.png` 可能会丢失，当执行到 `new Tray(iconPath)` 试图创建系统托盘图标时，一旦发现找不到图标，就会直接把整个 Electron 主进程给搞崩溃，导致界面连出来都出不来。

我刚刚紧急改了主程序 `src/main/index.ts`，做了两个保险措施：
1. **找不到图标就不强求**：如果找不到 `icon.png`，就用透明的图标代替，保证界面绝对不会崩溃！
2. **加上全局捕获网**：如果 Electron 主进程有任何崩溃的报错，现在都会用 `electron-log` 记下来，不会再“死得不明不白”。

我正在将这段“防崩溃代码”提交并推送上去，GitHub Actions 会马上打一个新包出来。等这个包出来后，再麻烦您覆盖安装一下看看能不能直接跑起来！(ง •_•)ง
