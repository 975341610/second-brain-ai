Agent: create_task(traecli: Commit sidecar debug logging improvements)
Agent: 日志里只有 "Starting sidecar process..." 和 "Sidecar working directory..."，**但是完全没有 `[Backend STDOUT]` 和 `[Backend STDERR]`！** 
并且后续也没有 `exited with code` 这种退出日志。
这说明子进程根本**没有吐出任何日志就卡住了**，或者**Python 进程的日志被缓冲(Buffered)拦截了没有输出到终端**。

我又发现了两个隐藏的暗坑，刚才已经写好了修复脚本，马上就能自动提交：
1. **Python 日志缓冲（最可能的原因）**：Python 默认会把输出缓冲起来，不直接吐给 Electron。我给系统加上了环境变量 `PYTHONUNBUFFERED: '1'`，逼着它一有屁就立刻放出来！
2. **Health Check 死等**：给 `/health` 接口加上了超时时间限制，防止请求一直挂起死锁。

我已经让小弟去提交和 Push 了，等 Actions 出新包之后，就算它卡死，这次的日志一定会有实打实的 Python 报错输出。我们离真相已经很近了！🔥
