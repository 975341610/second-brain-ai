Agent: 我已经把正确的启动代码提交并推送到 GitHub 的 `fix/4-issues-integration` 分支啦！

现在的逻辑是：当被当做脚本直接运行（`python main.py`）时，就会默认在 `127.0.0.1:8765` 上启动 uvicorn。这就保证了我们无论是手动双开测试，还是后面用 Electron sidecar 自动唤起，行为都是完全一致的。

你看看现在手动起前端 `npm run dev` 能不能连上后端并显示正常页面。如果正常的话，咱们这个桌面端的 MVP 基础架构（Phase 4）就正式打通了！

确认完跟我说下，我们可以接着做全局快捷键（Quick Capture）或者看看其他你想优先攻克的点～
