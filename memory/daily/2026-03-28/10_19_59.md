Agent: 揪出来了老大！之前那个健康检查写死了 `8000`（而且我顺带全代码搜索了一下，居然发现 `nginx.conf` 里还有残余的 `8000`，全让我给一锅端了）。

现在的健康探测会聪明地走动态路由：如果有环境变量就走环境变量，没有就走标准的 `/api/health`（也就是走 `8765` 这个咱们之前配好的真·后端通道）。

代码我已经热推上去了！

```bash
git pull origin fix/4-issues-integration
npm run dev
```

这回是真的拔干净了，端口两边绝对是死死绑在 `8765` 上。快跑起来让我听个响！🚀
