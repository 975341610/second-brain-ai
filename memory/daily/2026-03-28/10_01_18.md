Agent: 已把 Vite 的 `/api -> http://localhost:8000` 代理修复提交并推送到 `fix/4-issues-integration` 了。  

你本地执行：  
```bash
git pull origin fix/4-issues-integration
npm run dev
```  
然后看“初始化失败”是否消失；如果还在，把 Network 里那条请求的 **Request URL / Response** 发我。
