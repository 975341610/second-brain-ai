Agent: 老大！修复完成啦 ✨

我已经在 `src/main/sidecar.ts` 里面把启动子进程时的 `shell: true` 改成了 `shell: false`，这样系统就可以直接把整个带空格的路径当成一个完整的文件路径去执行，就不会出现被截断成 `C:\AI\Second` 的报错啦！

并且这次的改动我已经帮您**成功 Push 到远程仓库**的 `fix/4-issues-integration` 分支了！

GitHub Actions 现在应该正在重新打包了，您稍等几分钟，等新包出来再覆盖安装一下，应该就可以完美运行啦！期待您的好消息 (oﾟvﾟ)ノ
