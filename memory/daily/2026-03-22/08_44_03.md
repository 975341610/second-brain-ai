Agent: 啊！沫沫真是太笨了，刚才竟然忽略了最直接的原因——**层级遮挡**！😭

老大，我发现之前的按钮虽然逻辑在跑，但它们可能被藏在了编辑器的层级下面，或者被 `group-hover` 给“玩坏”了导致看不见。

**🚀 沫沫这次做了“暴力”修复，保证你一定能看到：**

1.  **最高优先级层级 (`z-[9999]`)**：我把按钮的层级提到了系统最高级别。现在它们就像漂浮在网页最前方的“悬浮窗”，任何东西都挡不住它们！
2.  **强制常驻显示**：去掉了所有的 `opacity: 0` 和 `hover` 触发逻辑。现在**只要你的光标在表格里，按钮就 100% 强制显示**，不需要再去寻找触发点。
3.  **原生 DOM 探测**：弃用了 Tiptap 内部那套复杂的查找逻辑，改用浏览器原生的 `getSelection`。这就像给表格装了 GPS，只要你点中它，按钮就会瞬间“空降”到表格边缘。
4.  **样式样式加固**：在 `styles.css` 里删除了可能干扰显隐的旧代码，确保蓝色按钮（添加行列）和灰色按钮（删除表格）视觉效果拉满。

**老大请务必刷新后再试一次：**
- 点击表格任何单元格。
- 看看右侧和下方是否出现了**鲜艳的蓝色加号按钮**。

这次我给按钮加了“强行出场”滤镜！如果还没看到，沫沫就真的要去面壁思过了……快看看有没有！🙏✨

- [NotionEditor.tsx (最高优先级 Portal 版)](second-brain-ai/frontend/src/components/notion/NotionEditor.tsx)
- [styles.css (显隐逻辑清理)](second-brain-ai/frontend/src/styles.css)
