
## 09:33:32
- 用户反馈 Notion 表格组件更新未生效，无法看到添加行列的按钮。
- 重构 `NotionEditor.tsx`：弃用 DOM 模糊匹配，改用 `nodeDOM` 钩子锁定表格坐标，增加窗口缩放 (Resize) 同步监听，并优化 `updateTableRect` 渲染性能。
- 针对按钮仍不可见的问题进行二次修复：
    - 将按钮层级提升至 `z-[9999]` 并移除 `opacity` 和 `hover` 触发逻辑，确保光标在表格内时强制常驻显示。
    - 改用浏览器原生 `getSelection` 替代 Tiptap 查找逻辑以提高定位精度。
    - 清理 `styles.css` 中可能干扰显隐的旧代码。
- 生成 AI 行业早报文件 `ai_daily_news_20260322.md`，涵盖 GPT-5.4、Qwen 3.5 等新闻。
- 修复表头菜单（...）点击无反应问题：将点击响应区域扩大至 `th` 右上角 40x40 范围，添加 `pointer-events: auto !important` 确保事件透传，并增强悬浮时的视觉反馈。

## 13:10:59
- 根据用户需求简化表格表头：隐藏了属性类型标签（如“Aa 文本”），但保留了表头右上角的菜单（...）按钮，确保用户仍可执行插入列或删除列的操作。
- 升级代码块组件功能：接入 `lowlight` (基于 Highlight.js) 实现多语言自动语法高亮。
- 优化代码块 UI/UX：增加了 Notion 风格的“红黄绿”窗口装饰、语言手动切换下拉框以及带状态反馈的“一键复制”按钮。

## 14:59:37
## 14:14:49
- 修复代码块组件 UI 间隙问题：移除 `pre` 标签默认 margin，去除输入区顶部的冗余边框，使标题栏与代码区视觉上无缝融合。

## 14:19:11
- 启动“万物皆可拖拽预览”功能开发，确立严格的 TDD (测试驱动开发) 协作模式：
    - 分设三个代理角色：Architect (计划与总览)、Developer (代码实现)、QA (自动化测试)。
    - 目标涵盖：拖拽上传、多媒体直接预览、自由缩放及布局调整。

## 14:28:02
- 完成第一阶段（图片/多媒体）TDD 架构设计与拆解：
    - 架构核心：引入 `MediaNodeView` 统一处理拖拽事件，根据 MIME 类型分发组件。
    - 待开发组件：规划 `MediaResizer` (缩放手柄) 和 `DraggableExtension` (拖拽扩展)。
    - 测试先行：优先编写 `drag-handle.test.ts` 和 `media-validator.test.ts` 确保文件拦截准确性。

## 14:39:19
- 根据用户反馈完善文件处理逻辑，增加“文件卡片兜底”机制：
    - 逻辑调整：针对大文件 (>50MB) 或不支持直接预览的格式 (如 .zip, .pdf)，不再强制预览。
    - 组件规划：设计 `FileBlock` 组件作为兜底展示，显示文件名与大小，并支持点击跳转或下载。

## 15:40:41
- 完成“万物皆可拖拽预览”第一阶段开发，并基于 TDD 模式通过了生产环境构建。
- 实现多媒体智能识别与渲染：支持图片、视频 (`mp4`, `mov`) 及音频 (`mp3`, `wav`) 的原生播放器或预览图展示。
- 落实文件卡片兜底机制：将超过 20MB 的大文件或不支持预览的格式（如 `.zip`, `.pdf`, `.docx`）自动转换为 Notion 风格的文件卡片，支持显示元数据及点击跳转/下载。
- 新增通用缩放功能：为所有媒体组件及文件卡片添加右下角缩放手柄，支持自由调整宽度。
- 扩展交互入口：除了拖拽上传外，新增 `/file` 斜杠指令以唤起本地文件选择框。
- 完善自动化测试：编写 `mediaUtils.test.ts`，覆盖类型识别、文件大小格式化及兜底判断等 7 种核心场景。

## 17:30:36
## 16:13:23 - 17:10:18
- 解决拖拽上传导致的 `413 (Request Entity Too Large)` 错误：
    - **存储重构**：放弃 Base64 编码直接存入笔记的方案，改为服务器端文件存储 (`data/uploads`)，仅在笔记中保留 URL 引用。
    - **网关限制规避**：针对 Nginx 网关默认上传大小限制（约 1MB），实现**分片上传 (Chunked Upload)** 机制，将文件切分为 512KB 的数据块传输。
- 优化大文件上传体验与逻辑：
    - 前端利用 `Blob URL` 实现“零延迟”本地即时预览，无需等待后台上传完成即可显示占位内容。
    - 后端新增 `/api/media/upload/init`、`/chunk`、`/complete` 三套接口，支持大文件的分块接收与合并。
- 排查并修复部署与环境问题：
    - 针对分片代码部署后仍报 413 错误的问题，定位为浏览器缓存了旧版脚本，执行了清理构建并发布新版前端 (`index-CsxLiBEc.js`)。
    - 修复 `/api/media/upload/init` 接口报 `405 (Method Not Allowed)` 问题，确认原因为后端服务未及时加载新路由，通过强制重启后端服务解决。

## 17:55:40
- 修复文件上传时的 `net::ERR_FILE_NOT_FOUND` 错误：
    - **原因定位**：本地 Blob 预览资源在后端链接替换完成前被过早回收（Garbage Collection）。
    - **延迟回收机制**：将 Blob URL 有效期延长（>5秒），确保编辑器完成 URL 替换后再清理临时资源。
- 优化上传交互体验与逻辑：
    - **状态同步改进**：增强编辑器内部查找替换逻辑，确保上传完成后占位符能精准切换为服务器正式链接。
    - **视觉反馈**：新增上传状态样式，上传过程中图片显示 50% 透明度，成功后恢复正常。
- 部署前端更新：重新编译并发布新版本 `index-CIXTMbAi.js` 以应用上述修复。

## 20:01:17
- **彻底修复 `blob: ERR_FILE_NOT_FOUND` 问题**：
    - **方案升级**：放弃单纯的延迟回收机制，将图片、音视频全量升级为 Tiptap **智能节点 (Node)**，增加 `data-upload-id` 标识。
    - **数据清洗**：在保存逻辑中增加拦截器，强制清洗所有 `blob:` 临时链接，确保数据库仅存储正式 URL，发布前端更新 `index-BWgvd40t.js`。
- **修复编辑器崩溃错误**：
    - **错误信息**：`Uncaught RangeError: Content hole not allowed in a leaf node spec`。
    - **解决方案**：修正 `FileNode` 定义，移除错误的文字插槽（hole）占位符，确立其为不可插入内容的原子叶子节点，发布前端更新 `index-BJOctyA1.js`。
- **实现视频链接自动嵌入**：
    - **功能描述**：新增 Bilibili 和 YouTube 链接粘贴监听，自动提取 ID 并转换为在线嵌入式播放器。
    - **状态确认**：确认 Bilibili 播放器脚本报 `bili-user-fingerprint` 错为第三方无害日志，不影响功能，发布前端更新 `index-BBp3phNv.js`。
- **版本发布与归档**：
    - 完成 **v7.1.0** 版本发布，编写更新日志 `second-brain-ai/CHANGELOG_v7_1_0.md`。
    - 推送代码至 GitHub，提交信息：`feat: implement chunked file upload and auto-embed video support (v7.1.0)`。
- **确立后续开发计划**：
    - 下一阶段将启动 **全站 UI & 布局重构**，重点涵盖侧边栏重构、极简排版优化及深色模式/毛玻璃风格探索。

## 21:01:47
- **修复回收站逻辑缺陷**：
    - **问题描述**：用户反馈网络错误（404/AxiosError）导致删除笔记失败，以及当父页面已被删除时，从回收站恢复子页面会导致该页面在笔记列表中不可见。
    - **解决方案**：优化恢复逻辑，检测到父页面不存在时，将恢复的子页面自动提升为**根目录笔记**，确保其在列表中正常显示。
- **增强回收站功能**：
    - 应用户需求，新增 **“一键清空回收站”** 功能（删除所有笔记）。
    - 新增单条笔记的 **“彻底删除”** 按钮。
- **解决服务部署问题**：
    - **故障排查**：用户在使用新功能时遇到 `405 Method Not Allowed` 错误。
    - **处理**：确认为新接口发布后后端服务未自动重启所致，通过手动重启后端服务解决。

## 22:16:28
- **修复笔记本侧边栏 UI 异常**：
    - **问题描述**：用户反馈笔记本列表的 `...` 菜单和添加笔记本的 `+` 按钮消失。
    - **修复内容**：恢复列表项悬停（Hover）触发逻辑，补全点击 `+` 号后弹出笔记本名称输入框的交互，并修正菜单的相对定位防止偏移。
- **优化 AI 助手 UI 与底层逻辑**：
    - **控制面板适配**：实现 AI 助手的“沉浸式嵌入模式”，移除多余边框阴影，将引用文献改为双列布局，压缩顶部操作区体积以融入 Dashboard。
    - **功能修复与增强**：
        - 修复 RAG 流式传输（Streaming）时不显示引用文献的 Bug（调整为后端先回传引用列表）。
        - 针对 Agent 模式强制采用非流式传输，确保 TODO 任务生成的结构化数据稳定。
        - 增强后端对 AI 返回 JSON 数据的解析健壮性（兼容 Markdown 代码块包裹的情况）。
- **扩展任务看板（Task Dashboard）功能**：
    - **需求实现**：响应用户对任务管理的优化需求，实现“废弃单个任务”和“一键清除已完成任务”。
    - **代码变更**：后端 `repositories.py` 新增 `delete_task` 和 `clear_completed_tasks` 接口；前端 `HomeDashboard` 新增垃圾桶图标与 Clear 按钮。
- **排查新接口部署问题**：
    - **故障记录**：用户在使用新开发的“一键清除已完成任务”功能时，浏览器控制台报错 `POST /api/tasks/clear-completed 405 (Method Not Allowed)`，提示新接口发布后可能未正确重载。
