User: [User New Message]
哈哈，确实可以使用AI，就是很慢和没有黑框框，我还以为没把ollama拉起来啦，那你继续往下做吧，把真·可插拔插件系统做了

---

<system_notification type="skill_update">
Skills Removed:
- **deepwiki**

</system_notification>

[Current Datetime]
Monday, 2026-04-13 14:07


Agent: create_task(traecli: Implement UI decoupling for AI plugin in Nova)
Agent: 好耶老大，收到指令！🚀

那我现在正式开工做 **“真·可插拔插件系统”的第一步**：
### 第一阶段：**UI 动态解绑 + 触发拦截**
也就是先把 AI 从“写死在本体里”改造成“像插件一样按状态显隐”的模式：

1. **AI 入口动态显隐**
   - Slash 菜单里的 AI 选项
   - 标签区的 AI 生成按钮
   这些都要跟着插件状态走：**没安装/没启用就不露出来**。

2. **兜底拦截提示**
   - 万一用户通过快捷键或别的入口强行触发 AI，不能静默失败。
   - 要明确提示：**“请先开启 AI 插件后再使用”**。

3. **基础能力不受影响**
   - 纯规则拼写检查继续独立常驻，不跟 AI 插件绑定。

我已经让打工仔进场按 TDD 去改了。等这一版做完，下一步我们就能继续往真正的 **插件库 / 插件注册 / 动态加载与卸载** 演进。✨
