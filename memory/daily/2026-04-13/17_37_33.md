User: [User New Message]
> 老大，词库可视化导入和热更新的代码已经成功推送到 `main` 啦！现在本地环境非常干净。
> 
> 接下来，咱们直奔今天的最终大招——**真·插件系统 (Phase 2)**！
> 
> **Phase 2 的核心目标：真正的“物理级”拔插解耦**
> 以前的开关只是前端隐藏了按钮，后台的 Ollama 进程依然在偷偷吃内存。
> 这次我们要做到：
> 1. **物理卸载**：当你在设置里把“AI 插件”一关，前端立刻呼叫后端，**后端直接把 Ollama 的底层进程（甚至占用显存的模型）强制 Kill 掉**，释放所有 CPU 和内存！
> 2. **按需加载**：当重新打开 AI 插件时，后端在静默状态下重新拉起 Ollama 服务，整个过程无缝衔接。
> 3. **接口打通**：前端的开关状态和后端的进程存活状态强绑定，真正实现“不用不占资源”的商业级绿色软件标准。
> 
> 我这就派专员去动刀后端的进程管理和路由接口。代码依然只留本地，修完向你汇报！🚀

报错啦
Failed to load resource: the server responded with a status of 405 (Method Not Allowed)
SettingsDialog.tsx:52 Failed to import dictionary: Error: {"detail":"Method Not Allowed"}
    at invoke (api.ts:88:11)
    at async handleImportDict (SettingsDialog.tsx:48:19)
handleImportDict @ SettingsDialog.tsx:52

---

<system_notification type="skill_update">
Skills Removed:
- **deepwiki**

</system_notification>

[Current Datetime]
Monday, 2026-04-13 17:36


Agent: 收到老大，这个报错我看明白了：前端点击“导入词库”时，请求是发出去了，但后端返回了 **405 Method Not Allowed**，说明**路由路径、请求方法或 IPC 映射**里至少有一个没对上。

我先按最可能的三处去排：
1. **前端 `invoke` 映射**：`text:dictionary:import` 这个 IPC 通道有没有真的按 `POST` 转到后端。
2. **后端路由挂载**：`/text/dictionary/import` 虽然定义了 `POST`，但有没有被实际挂到当前运行的 router 上。
3. **桌面端代理层**：Electron / 本地 bridge 有没有把这个请求错误地当成 GET 发出去了。

我现在立刻修这个 405，先把“能正常导入”恢复。修完先本地给你汇报，不私自 Push。
Agent: create_task(traecli: Fix dictionary import 405 error)
