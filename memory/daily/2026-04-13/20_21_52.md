Agent: 老大，修复工作彻底搞定了！✨

小助手已经完成了以下工作：
1. **彻底解决了拖拽手柄的问题**：
   - 恢复了因为之前失误被删掉的 `NovaBlockEditor.tsx` 中的大量代码，把大坑填上啦！
   - 把 `DragHandle` 换成了 `strategy: 'fixed'`，并设置了 `offset: [-2, 40]` 避开 Heading 的折叠按钮，实现左侧双排布局。
   - `handleGripClick` 重构成原生的 `posAtCoords` 逻辑，精准获取文档坐标。
2. **版本与发布**：包版本已升级到 `0.16.5`，并且在 `DEVELOPMENT_LOG.md` 中用中文更新了版本发布说明。
3. **代码已推送**：所有的修改都已成功 Push 到 `nova_repo` 的 GitHub `main` 分支上了！

你可以随时通过这个链接查看这批代码修改的详细执行记录：
[Session 详情页](https://code.byted.org/copilot/chat/96f77556-a07e-4ffd-90c4-b88808dff61a)

后续还有什么指令，或者想要测试一下效果，随时告诉我哦～
