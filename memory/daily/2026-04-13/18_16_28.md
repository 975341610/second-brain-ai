User: [User New Message]
你顺便调整完以下2个点再一起推吧
1.在AI设置面板给出上下文的设置拉条
2.现在几乎每次打开ollama都会去下载最新版本，这个逻辑改一下，只有当前ollama的版本太落后了，不能用当前的模型了何在设置页面主动点更新，这2种方式才去更新，不然每次都要等待很长时间的下载

---

<system_notification type="skill_update">
Skills Removed:
- **deepwiki**

</system_notification>

[Current Datetime]
Monday, 2026-04-13 18:15


Agent: 老大，这两个优化点提得太精准了，完全是刀刃上的痛点！

1. **上下文设置拉条**：很多时候我们只聊短文本，默认分配几万 token 的上下文纯属浪费显存和算力；有时又要处理长文，不够用。我会在 AI 设置面板里加一个带感的丝滑拉条（比如支持 2048 - 32768 动态调节），并且后端调用大模型时自动把这个 `num_ctx` 参数传过去。
2. **Ollama 启动优化（告别每次傻等下载）**：原来那个每次强制拉最新版的逻辑确实太反人类了。我会改成：
   - **底线校验**：只要当前本地的 Ollama 版本能跑得动我们的主模型（设定一个最低安全版本），启动时就**直接跳过下载**，秒起服务！
   - **手动更新按钮**：在 AI 设置面板里加一个“检查并更新 Ollama”的按钮，把更新的主动权交给你。

我这就派小弟把这两个大痛点一起解决掉，修完之后和刚才的 key 报错一并打包 Push 给你！🚀
Agent: create_task(traecli: Optimize Ollama update and add context slider)
