Agent: 老大，这个重复 key 的小 Bug 已经干掉了！

**原因很简单**：
以前前端的标签列表直接用了“标签名”（比如“奇幻”）作为 React 的唯一 Key。但是因为咱们从数据库或者 AI 建议里拿到的数据，可能包含相同的标签（比如你手动打了个“奇幻”，AI 又给你推荐了一个“奇幻”，或者首尾多了个空格“奇幻 ”），导致同一个名字在数组里出现了两次，React 渲染 DOM 树的时候就不知道谁是谁了，直接报错。

**我刚才做的外科手术**：
1. **源头活水去重**：只要拿到标签数据（不管是初始化的，还是 AI 推荐的），我都在代码里强制加了一道 `Set` 去重过滤。
2. **AI 智能去重**：当你点击那个“✨智能建议”时，如果 AI 吐出来的标签，你文章里已经有了，它就会被静默抛弃，绝不会在下面生成两个一模一样的按钮。
3. **输入框防呆**：去掉了首尾空格导致的误判（`trim()`）。

这下控制台肯定清爽了。老大你在本地随便加点重复的标签或者点一下 AI 建议试试，没问题的话，我再打包给你 Push 进去！🚀
