Agent: 老大，搞定了！✨

我已经按照你的方案，把代码干净利落地回退到了 `ea215b6b004eb0d640993c3942dc721bd0cc6073` 这个基线版本，然后在这个纯净的底座上重新把块手柄的问题修好了。因为回退到了健康版本，之前那个 `@xyflow/react` 的依赖报错也随之彻底消失，本地的 Build 构建验证已经顺利通过！

**这次结构化修复的核心点如下：**
1. **彻底解决 Y 轴漂移**：在滚动容器上增加了滚动事件的强制同步，让底层 Tippy.js 实时重算手柄位置，同时锁定了 `placement: 'left-start'`，现在手柄在长文档里滚动时稳如泰山。
2. **解决手柄与标题折叠按钮重叠**：重新做了物理隔离布局。把拖拽手柄向左侧进一步偏移（`[-4, 44]`），并调整了标题折叠按钮的内边距，实现了“最外层手柄 - 中间折叠按钮 - 内层文本”的清晰分层。
3. **交互命中逻辑优化**：通过调整层级（`zIndex` 和 DOM 挂载点）及扩大命中区域，保证现在点击和拖拽手柄时既灵敏又准确。

所有的修改记录也已经按照规范更新到了 `DEVELOPMENT_LOG.md` 里。

[详细执行记录点这里查看](https://code.byted.org/copilot/chat/c8bcd439-d3ae-42b1-baf1-88194b8f0158)

目前所有代码都已经 Commit 并在本地就绪，**还没有推送到远程**。老大，你这边确认没问题的话，我这就直接帮你 Push 到 `main` 分支？🚀
