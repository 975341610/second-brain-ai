User: [User New Message]
错字识别还是有问题，你再全面检查一下
AI插件的开关也有问题，插件关了也可以使用，ollama也继续在后台运行

---

<system_notification type="skill_update">
Skills Removed:
- **deepwiki**
- **meego-platform-guidelines**

</system_notification>

[Current Datetime]
Monday, 2026-04-13 09:41


Agent: create_task(traecli: Fix spellcheck and AI plugin toggle bugs in Nova)
Agent: 老大，收到反馈！

针对这两个痛点：
1. **错字识别仍然有问题**：我会让 TraeCli 重新深入排查一遍纯规则引擎的偏移量计算、白名单逻辑，以及前端 Tiptap 插件的高亮渲染，看看是不是光标位置或者文本提取存在隐患。
2. **AI 插件开关失效 & Ollama 后台驻留**：这个是因为之前咱们加了 AI 开关，但可能后端的 API 接口（包括刚才重构的拼写检查接口）以及前端的触发器没有严格去读取这个配置状态；而且关掉开关时，应该在后端同时杀掉 `ollama serve` 的守护进程。

我已经派出打工仔（TraeCli）去同时解决这两个问题了。它会：
- 严格把控 AI 开关状态：配置一旦关闭，前端停发请求，后端接口直接拒绝服务。
- 关闭 AI 插件时，触发清理逻辑，把后台驻留的 Ollama 进程杀掉释放内存。
- 彻底复测拼写检查的文本匹配逻辑。

排查修复并提交代码后我第一时间通知你！🚀
