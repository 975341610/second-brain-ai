Agent: 老大，刚才提的三个痛点全干掉了！这波直接给 Nova 来了个“性能解压套餐”！

**修复和优化内容：**
1. **PropertyPanel 报错清空**：`React key` 重复的问题已经通过在前端做 `Set` 强去重和智能过滤彻底解决了。不管你手动打多少个相同的标签，或者 AI 推荐了文章里已有的标签，控制台都不会再乱报错，界面也不会错乱。
2. **Ollama 秒起（告别强制下载傻等）**：后端检测脚本改了。现在只要本地安装的 Ollama 版本大于或等于底线要求（默认 `0.1.29`），引擎就会**秒起跳过下载**！不会再每次点开都傻乎乎地去连 Github 下安装包了。
3. **上下文拉条 & 手动更新按钮**：
   - AI 设置面板里加了一个很带感的**上下文长度（Context Length）滑块**（范围 2048 - 32768，默认 8192），直接物理透传给 Ollama 的 `num_ctx` 参数！长文短文随你调配，显存你做主。
   - 还在面板最下面加了一个“**检查并更新 Ollama 版本**”的手动按钮。平时秒起，只有你想更新了，点一下它才会去拉最新包，彻底把控制权交还给你。

你现在可以在本地切到设置里拉一拉滑块，或者关掉重启一下本地开发服务器，感受一下 Ollama 不再疯狂下载的秒起快感！

要是没问题，我就把这波超级体验升级（Hotfix + Ollama 优化）打包 Push 上去！🚀
