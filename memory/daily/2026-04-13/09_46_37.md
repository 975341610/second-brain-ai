Agent: 老大，打工仔（TraeCli）已经完成修复并把代码 Push 到 GitHub 啦！🎉 

这两大顽疾都被彻底拔除了：

### 1. 错字识别不准/乱标的修复
- **前端计算逻辑彻底重构**：之前红线乱标是因为前端自己在做字符串查找（`indexOf`），遇到重复的字就容易错位。现在前端**直接使用后端引擎返回的精确字符偏移量（`offset`）**进行高亮渲染，百分之百精准定位！
- **后端正则防误报**：改进了正则匹配，排除了诸如“我的”、“你的”这类代词前缀，极大降低了“的地得”规则的误报率。并且补充了复杂的重叠匹配测试。

### 2. AI 插件开关 & Ollama 驻留的修复
- **前端拦截**：现在当你在设置里关掉 AI 插件时，前端（包括 Tiptap 的拼写检查、标签生成等）会在执行前判断状态，一旦关闭，直接“截断”网络请求。
- **后端拦截与清理**：
  - 后端所有 `/api/ai/*` 接口都增加了 `ai_enabled` 的强制校验，关闭状态下直接拒绝服务。
  - **核心修复**：后端服务新增了 `shutdown()` 生命周期机制，当检测到 AI 插件被关闭时，系统会**主动杀死（kill）后台悄悄运行的 `ollama.exe` 进程**，彻底释放你的显存和内存！

老大，你现在可以：
```cmd
cd C:\AI\Nova
git pull origin main
```
拉取一下最新代码，重启一下后端，再去开关一下 AI 插件或者输入一些特定错字试试看！有任何问题随时反馈给我！✨
