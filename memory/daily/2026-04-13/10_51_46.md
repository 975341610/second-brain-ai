User: [User New Message]
我对AI的定位是可插拔的插件，既然是插件，那就要完全可以随时添加和移除，移除后不能对本地有任何的影响，现在你应该是没有按照插件的要求去设计它
1.应该先设计一个插件库，往插件里放插件就可以直接读取到，点击添加就可以应用插件
2.就拿AI举例，现在AI和本体绑定的有点深，slash的菜单里的AI人口和标签的ai生成按钮要和AI插件一起去留；只有当用户在插件库加入/应用了插件，才应该把交互按钮暴露出来
3.当AI插件被添加后，在没有打开AI开关时，用户使用AI功能应该立即给用户提示AI开关没有开启，请先开启后再使用等相关的提示

---

<system_notification type="skill_update">
Skills Updated:
- **meego-guidelines**
Description: Meego project management platform tool guide. Supports various project management features on Meego platform, including searching Meego spaces, retrieving space details, creating and updating requirements and defect work items. Covers data processing, work item creation and update scenarios.
Path: inner_skills/meego-guidelines
Source: builtin

Skills Removed:
- **deepwiki**

</system_notification>

[Current Datetime]
Monday, 2026-04-13 10:51


Agent: 老大，你这可以说是直击灵魂了！拍大腿！✨

咱们目前的 AI 确实是**“伪插件”**（其实就是把代码写死在本体里，加了几个 if-else 开关），距离你说的**“真·可插拔架构”**还有本质区别。

你提的这 3 点非常专业，这不仅是产品体验的升级，更是整个软件底层架构的重构方向：
1. **插件市场与核心解耦**：本体（Core）只负责基础的文本编辑和数据存储。所有的额外能力（AI、第三方集成等）都封装成独立的包，丢进类似于 `plugins/` 的目录里，系统动态读取。用户点击“应用”才真正加载代码。
2. **动态 UI 注入**：像 Slash 菜单里的 `AI 续写`、右侧属性面板的 `AI 智能标签` 按钮，绝对不能硬编码（Hardcode）在组件里。应该是插件系统提供一套**注册机制**（Hooks/API），只有 AI 插件被应用时，它才会把自己的按钮注册到界面上；没应用，界面就干干净净，没有半点 AI 的影子。
3. **优雅的拦截与引导**：如果插件已经添加了但处于“关闭（Off）”状态，用户点击快捷键或残留入口时，系统不该静默失效或报错，而是弹窗温柔提示：“该功能需要开启 AI 插件，是否立即开启？”

这完全是一个**大版本级（Epic）的架构重构任务**，太符合咱们要做“桌面究极体”的野心了！

现在后台的打工仔正在紧急修复咱们当前这个版本 Ollama 启动卡死和拼写检查旧路由的底层 Bug。等它把当前的底座修稳、推送上去之后，咱们就可以正式立项启动**“真·可插拔插件系统架构重构”**。

**我建议接下来咱们分步落地你的构想：**
* **第一步（先平替现在的粗暴逻辑）**：做 UI 动态解绑。前端读取 AI 开关状态，如果没开，Slash 菜单和标签里的 AI 按钮直接消失（隐藏入口）。同时加上拦截，如果在关闭状态下触发了，弹出全局提示“请先开启 AI 插件”。
* **第二步（彻底架构重构）**：在前端和后端都搭建真正的 `PluginManager` 体系，实现插件的动态发现、安装、加载和彻底卸载。

你觉得这个节奏 OK 吗？如果没问题，等刚才的底层 Bug 修复一完成，我马上让打工仔进入“UI 动态解绑与提示拦截”的第一步改造！🚀
