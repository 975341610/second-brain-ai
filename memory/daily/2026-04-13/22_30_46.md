Agent: 代码已经修改并编译成功啦！✨

我修复了以下两个问题：
1. **标题收起按钮与拖拽手柄重叠**：我将 `HeadingView.tsx` 中收起按钮的位置完全移到了左侧外部（`-left-[2rem]`），并且在 `NovaBlockEditor.tsx` 中把拖拽手柄向左拉开到 60px 的偏移量。这样它们在视觉上和交互区域上就分开了。
2. **Slash Menu 自动消失的问题**：我在 `SlashMenuConfig.tsx` 中增加了一层 `requestAnimationFrame` 防抖处理，避免因为界面抖动导致 Tippy 计算位置失效而立刻隐藏；同时当没有匹配项时，它也不会直接 `return null` 然后破坏弹窗状态了。

目前的开发服务器已经在运行中（您可以直接在这个页面预览测试啦：[前端预览 URL](https://iris_4313d091-e484-4384-8564-8ce91e478bc4-5179.aime-app.bytedance.net/)）。
*(注意：需要带上 token / 相关鉴权正常使用你的笔记页面)*

老大，麻烦您测试一下这几个地方手感：
1. **拖拽手柄位置**：鼠标悬停在普通段落和标题（H1~H3）时，左侧的六个点拖拽手柄和标题的折叠三角形是不是**完全分开了**？
2. **Slash Menu (`/`)**：输入 `/` 后，菜单是否能**稳定驻留**，不会一边打字一边闪退了？

确认没问题的话，跟我说声“Push 吧”，我再把这些改动推送到您的 GitHub！🚀
