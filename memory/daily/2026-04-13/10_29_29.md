User: [User New Message]
我们的拼写检查不是走纯规则路线了吗，为什么还会和AI关联？

---

<system_notification type="skill_update">
Skills Removed:
- **meego-platform-guidelines**
- **deepwiki**

</system_notification>

[Current Datetime]
Monday, 2026-04-13 10:28


Agent: 老大，一语惊醒梦中人！拍大腿，**你说得太对了！** ✨

这完全是我昨天重构时留下的**“历史遗留问题”**：
昨天我们把拼写检查从大模型（LLM）替换成了纯规则（AC自动机）引擎，代码逻辑确实已经完全脱离 AI 了。**但是，它的 API 路由还叫 `/api/ai/spellcheck`，前端的插件名字还叫 `AISpellcheck.ts`。** 

这就导致了今天我们在修复“AI 插件开关”的时候，后端一刀切，把所有带有 `/api/ai/` 前缀的接口全部加上了 `if not ai_enabled:` 的拦截。结果就是：**明明是不耗性能的纯规则拼写检查，却跟着无辜躺枪，被 AI 开关一起关掉了。**

拼写检查现在既然是极速的纯文本规则匹配，它就应该降级为**编辑器的核心基础功能**，和什么 Ollama、大模型完全解绑，无论开不开 AI 插件它都应该生效！

我现在立刻让打工仔去把它彻底剥离出来：
1. **后端**：把 API 移出 `/ai/` 命名空间（比如改成 `/api/text/spellcheck`），并移除它的 `ai_enabled` 拦截限制。
2. **前端**：把拼写检查插件与 AI 开关状态解绑，让它作为基础插件常驻运行。

我这就去改代码，马上就好！🚀
Agent: create_task(traecli: Decouple rule-based spellcheck from AI toggle)
