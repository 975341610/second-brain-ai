Agent: 老大，UI 动态解绑（Phase 1）的代码已经提交并推送到 main 分支了。现在的状态是：当 AI 插件关闭时，界面的智能标签按钮和 `/` 菜单里的 AI 选项都会自动隐藏。

接下来，我全副精力投入到**错别字检查（Spellcheck）没反应**的深度排查中。
我会派专员重点盯这几个地方：
1. **前端触发机制**：`compositionend`（中文输入法结束）和 `update` 事件是不是真的把文本发给后端了。
2. **中文字符位移（Offset）计算**：后端的 Python 算出来的错字位置，拿到前端 ProseMirror 里能不能精准对上（这是最容易导致红线画不出来的坑）。
3. **规则引擎匹配**：看看是不是阈值太严或者字典没配对。

排查任务已经发车，有实质性进展或找到病根我会马上汇报！
Agent: create_task(traecli: Debug and fix spellcheck feature)
