User: [User New Message]
有报错

PropertyPanel.tsx:248 Encountered two children with the same key, `奇幻`. Keys should be unique so that components maintain their identity across updates. Non-unique keys may cause children to be duplicated and/or omitted — the behavior is unsupported and could change in a future version.
<span>
（匿名）
@
PropertyPanel.tsx:248
PropertyPanel
@
PropertyPanel.tsx:247
3

PropertyPanel.tsx:248 Encountered two children with the same key, `奇幻`. Keys should be unique so that components maintain their identity across updates. Non-unique keys may cause children to be duplicated and/or omitted — the behavior is unsupported and could change in a future version.
<span>
（匿名）
@
PropertyPanel.tsx:248
PropertyPanel
@
PropertyPanel.tsx:247

---

<system_notification type="skill_update">
Skills Removed:
- **deepwiki**

</system_notification>

[Current Datetime]
Monday, 2026-04-13 18:09


Agent: 收到老大，这个报错很明确：`PropertyPanel` 里渲染标签列表时，用了标签名当 React key，而现在出现了重复值 `奇幻`，所以 React 在 diff 的时候炸警告了。

我这就去修：
1. 定位 `PropertyPanel.tsx` 第 247/248 行附近的列表渲染。
2. 查清楚是**数据源重复**，还是**前端 key 设计有问题**。
3. 改成稳定且唯一的 key（必要时做去重），保证不影响标签展示和后续编辑。
4. 顺手把 `DEVELOPMENT_LOG.md` 补上这次 hotfix 记录。

先本地修，不会私自 Push。
Agent: create_task(traecli: Fix duplicate React key in PropertyPanel)
