User: [User New Message]
AI的开关，打开后不会启动ollama吗？
[!] Ollama not reachable: All connection attempts failed
[*] MOCK_LLM_ERROR detected. Attempting to connect to local Ollama (127.0.0.1:11434) using model 'gemma:2b'...
[!] Failed to connect to Ollama: All connection attempts failed
INFO:     127.0.0.1:53025 - "GET /api/ai/plugin-status HTTP/1.1" 200 OK
INFO:     127.0.0.1:53025 - "POST /api/ai/spellcheck HTTP/1.1" 200 OK

---

<system_notification type="skill_update">
Skills Removed:
- **meego-platform-guidelines**
- **deepwiki**

</system_notification>

[Current Datetime]
Monday, 2026-04-13 10:41


Agent: 老大，你这个日志很关键，已经暴露出两个现象了：

1. **AI 开关打开后，没有把 Ollama 真正拉起来**，所以后端在尝试连 `127.0.0.1:11434` 时直接报 `Ollama not reachable`。
2. 你这里依然打到了 `POST /api/ai/spellcheck`，说明你本地前端很可能还是旧包/旧缓存，或者 Electron IPC 还没切到新的 `text:spellcheck` 通道。

按理说，AI 开关打开后，如果设计是“本地 AI 模式”，就应该负责：**启动 Ollama 服务 / 检查 Ollama 可达 / 准备模型**，而不是只改一个布尔状态。

我现在就去把这条链路全面检查一遍：AI 开关 -> 后端 toggle 接口 -> Ollama 启动逻辑 -> 前端状态同步 / spellcheck 实际命中路由。修好后我给你一个明确结论和补丁。
Agent: create_task(traecli: Fix AI toggle not starting Ollama and verify spellcheck route)
