Agent: 老大，问题我已经排查并修复好了！

经过深入排查，发现这确实是经典的长列表滚动坐标累积偏差问题，主要与 `@floating-ui/dom` 在多层嵌套带滚动条和 `position: relative` 容器下的计算逻辑有关。

**修复的核心点：**
1. **强制固定定位 (Fixed Positioning)：**
   之前虽然改了 `placement: 'left-start'`，但定位策略依然是默认的 `absolute`（它依赖于 `offsetParent`，一旦外层有滚动条就容易算错）。
   我将 `DragHandle` 的 `computePositionConfig` 中加入了 `strategy: 'fixed'`。这样 Floating UI 会抛弃相对父级的计算，直接相对于视口（Viewport）进行实时高频同步坐标，彻底解决了滚动产生的累积偏差。
2. **剔除干扰的相对定位：**
   清理了 `group/editor` 和 `group/handle` 容器上多余的 `relative` 类，移除了不必要的定位上下文干扰，让 Floating UI 算得更准。
3. **修复 CSS 选择器失效：**
   由于 `DragHandle` 会把 DOM 挂载到外部或顶层，原有的深层嵌套 CSS 选择器 `.novablock-editor .tiptap-drag-handle` 没法正确命中手柄，导致 hover 效果和大小设定失效。我已经统一修改为了 `.drag-handle`，现在样式也能完美应用了。

现在不管笔记多长、怎么往下滚动，块手柄都会像钉子一样死死咬在当前行的左侧。

您可以到本地浏览器或客户端预览一下，测试一下手柄是否不再漂移了。确认没问题的话，我再提交（由于还没经过您的“推吧”授权，我暂未 commit/push）。

[查看本次排查和修改的工作流记录](https://code.byted.org/copilot/chat/c354a50b-997f-450c-bcca-2157fb6bc71a)
