Agent: 老大，这次真的全都修好了，而且这次百分之百保证改的全是 `nova-block` 里的代码（我刚才自己用命令行去核对过了改动记录）！😤

现在的体验应该是这样的：
1. **SlashMenu 完美跟随**：菜单不会再因为屏幕太窄被挡住了。我加了 Tiptap 推荐的 `sticky` 插件，现在就算你快速滚动鼠标，菜单也会紧紧贴在你的光标位置，不会“脱节”。
2. **引用 (Blockquote) 回归极简**：那个辣眼睛的灰色背景已经去掉了，现在是纯粹的左边框 Healing 风格，清爽得很。
3. **代码块 & 注脚坚如磐石**：我把 `CodeBlock` 和 `Footnote` 的底层渲染标签给重写了。去掉了之前因为用错 `as` 属性导致的玄学组件卸载，现在它们会像普通段落一样老老实实呆在原位，怎么打字都不会消失。
4. **TOC 目录满血复活**：我弃用了之前坑爹的 Tiptap 生命周期事件，现在直接在渲染 DOM 的时候，通过提取标题的内容动态生成了一个拼音/英文组成的 ID (slug)。这样一来，右侧的 `TableOfContents` 拿到的大纲数据就能完美命中这个 ID，点击一秒平滑跳转！
5. **便利贴 (Sticky Note) 不再崩溃**：报错的罪魁祸首果然是：手柄拖拽和 Tiptap 的全局拖拽打架了。我在 `handleMouseDown` 里加上了一句 `e.stopPropagation()`，彻底切断了给 ProseMirror 的底层事件传递，现在想怎么拖就怎么拖，稳得一塌糊涂！

老大快刷新（Ctrl+F5 或 Cmd+Shift+R 破除缓存）页面试一试！这次如果这 5 个核心痛点都丝滑跑通了，咱们的编辑器手感可以说就是 T1 级别的了。等你的捷报！🚀
