Agent: 老大，破案了，也修好了！✨

刚才的原因是：虽然我在 `tiptapExtensions.ts` 里给代码块挂载了 `lowlight` 高亮，但偏偏漏了最上面的两行 import 和初始化。

现在我已经把 `createLowlight` 给正确地挂在配置上了。而且，我还顺手把之前 `NovaBlockEditor.tsx` 里头残留的一些多余引用的 TypeScript 构建警告给清理掉了。

现在 `npm run build` 和 `npm test` 都完美绿灯通过了！页面也肯定不会再白屏报这个错了。

快去网页里狠狠刷新（Ctrl+F5/Cmd+Shift+R）试一把，看看代码块（输入 \code 或者 ```）是不是能立刻跳出来高亮，而且光标也稳如老狗了？等你捷报！🚀
