Agent: 可以，收到授权。

我查完确认：**已经全部推到 GitHub main 了，而且 main 跟远端完全同步**，所以刚才那个“再 commit + push”其实不需要——改动已经在 main 里了。

你现在 `git pull` 后跑 `fast_update.bat`，就会拿到：
- **表格光标问题修复**（commit: `10930c4`）：不需要先点进表格，按钮也能增删行列。
- **v0.5.4 + 移除拖拽**（commit: `ec072aa`）。
- **AI 流式错误透传**（commit: `0ff9279`）。

AI 这条 `All connection attempts failed`：下一步请你直接跑根目录的 `python test_ai_connection.py`，把输出完整贴我（里面会分段告诉我们是 DNS、TCP、还是 TLS/代理拦截）。我看一眼就能断定是不是需要 OpenAI-compatible 网关，还是纯网络不可达。
