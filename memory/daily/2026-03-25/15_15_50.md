Agent: 老大，两个问题都修复完毕并推送到 GitHub 啦！

1. **表格修复**：之前拖拽卡死、删行后再加行异常的原因是因为 ProseMirror 的事务和选区（Selection）状态丢失或死循环了。现在做了边界保护和强制修复。

2. **AI 11001 网络修复**：我在后端做了一次大手术：
   - **URL 自动规整**：如果你填的 `base_url` 没有 `http://` 或 `https://`，它会自动帮你补上。如果你不小心把 `/v1/chat/completions` 这类路径也填进去了，它会自动剔除。
   - **DNS 预检拦截**：现在在发起对话前，程序会先在底层用 `socket.getaddrinfo` 去查一下你填的域名。如果查不到（也就是触发 11001 错误），会直接返回明确的错误给前端，告诉你应该怎么排查（比如让你跑一下 `nslookup` 看看是不是被 VPN/代理拦截了）。
   - **可观测性日志**：在黑窗口（后台）里，现在会明确打印出它到底试图去连哪个 URL（脱敏处理）、有没有被环境变量里的代理影响。

你可以拉取最新代码（跑一下一键更新脚本）试一下，如果 AI 还有问题，看看界面上或者后台给出的提示和诊断建议。如果有新情况随时喊我！
