Agent: 老大，`superpowers` 凯旋归来！🎉 TDD 闭环完美收官，测试全绿！

**这波我们扫掉了好几个大雷：**
1. **底层引擎崩溃修复**：以前的 `moveRow` 粗暴乱塞数据，现在老老实实用 `Fragment.fromArray` 包装，ProseMirror 底层再也不会报那种莫名其妙的红错了，拖拽丝滑且不破坏原有撤销功能。
2. **拒绝“显卡杀手”**：我们加了 `lastHoveredRef` 节流器。以前你鼠标一划过表格，界面就疯狂重绘几百次；现在只有当你真正跨过表格行的边界时，才会触发一次轻量级重绘。
3. **消除“卸载后报错”**：清理了“幽灵定时器”，切换笔记时再也不会在后台疯狂报错了。

**自动化测试战报：**
- 新增 `tableCommands.test.ts` (跨行拖拽、排序验证) —— **通过 ✅**
- 新增 `tableBasics.test.ts` (原本的增删行列回归防退化) —— **通过 ✅**
- 总计 13 个测试用例，`100% GREEN` 🟢！

目前所有这些堪称艺术品的代码都放在安全的 `fix/table-drag-tdd` 分支里了。

老大，**我是直接帮你合进 `main` 分支并推送到 GitHub 呢，还是你想先在本地预览一下？** 听你发号施令！🦸‍♀️✨
