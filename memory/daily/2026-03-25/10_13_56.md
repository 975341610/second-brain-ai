Agent: 老大，破案了！🕵️‍♂️✨

**“无响应且无报错”的罪魁祸首找到了：**
咱们的 AI 客户端原本只认标准的 SSE 格式（必须以 `data:` 开头）。但你之前提到的那种 `sub2api` 或者一些第三方的 OpenAI 兼容接口，它们有时返回的是纯 JSON 行（直接是 `{...}`，没有 `data:` 前缀）。
这就导致咱们的解析器“眼瞎”了，把所有真实数据都跳过了，一直在死等标准格式的结束符。偏偏咱们的日志级别设成了 ERROR，跳过数据的警告（Warning）没打印出来，所以就成了“无响应、无报错”的灵异事件。

**我的修复方案（方案 A）：**
1. **智能兼容**：让解析器同时支持标准 SSE 和 JSONL 格式，管它带不带 `data:` 前缀，统统拿下！
2. **防干等机制**：增加“首个 Token 超时”兜底，万一卡住也会直接报错，不会让前端傻等。
3. **日志降级**：把日志调到 Warning 级别，以后再有奇葩数据也能看得到。
4. **测试验证**：写一个本地模拟服务器和测试脚本，专门针对这两种格式进行极速验证。

老大，如果没问题的话，回我一句**“按 A 继续”**，我立刻让 subagent 把代码改好并测试通过！🚀
