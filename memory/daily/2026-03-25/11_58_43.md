Agent: 老大，**“数据导入”功能已经按 `superpowers` 的 TDD 流程完美搞定了！** 🎉

**这次升级的亮点：**
1. **防爆雷机制**：在开始复制数据前，后端会强行执行 `engine.dispose()` 断开一切对当前数据库的占用，这样在覆盖文件时绝对不会出现 Windows 下常见的“文件被占用”报错。
2. **高危警示 UI**：在设置页面里，导入按钮不仅放在了一个带红色警示边框的区域，点击时还会弹出强提醒：“此操作将不可逆地覆盖当前所有数据”。
3. **重启提醒（方案B）**：数据覆盖成功后，前端会明确弹窗告诉你“导入成功，请彻底关闭并重新打开软件以生效”。
4. **全绿测试**：后端专门为这个接口写了沙盒测试（无效路径、空文件夹、正常导入等各种边界条件），全部通过！

目前这部分代码我已经写好并存放在本地的 `feat/data-import` 分支里了。

老大，那两行强制同步的 Git 命令你跑完了吗？“幽灵版本”的问题解决没？
如果那边搞定了，你给我个指令，我马上就把这个新功能合入主分支并推送到 GitHub 让你体验！🦸‍♀️✨
