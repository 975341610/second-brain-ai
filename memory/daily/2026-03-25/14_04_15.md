Agent: 老大，搞定了！漏掉的 `TextSelection` 已经被我按最规范的 `@tiptap/pm/state` 补进去了。而且刚才我已经让系统自己跑了一遍 `npm run build`，编译完全顺畅！

你现在只需要：
1. `git pull origin main`
2. 再次运行 `fast_update.bat` 

这下肯定能一路绿灯跑完打包了！抱歉之前漏了这个小尾巴，让你又见红了🙏 快试试！✨
