User: [User New Message]
src/components/notion/tableCommands.ts:68:28 - error TS2552: Cannot find name 'TextSelection'. Did you mean 'selection'?

68            tr.setSelection(TextSelection.near(resolvedPos));
                              ~~~~~~~~~~~~~

  src/components/notion/tableCommands.ts:15:11
    15   const { selection } = state;
                 ~~~~~~~~~
    'selection' is declared here.


Found 1 error.

[Current Datetime]
Wednesday, 2026-03-25 14:02


Agent: 收到，这个是我这边漏了一个显式 import，导致你本地 `tsc` 编译直接挂了。

`TextSelection` 属于 ProseMirror 的 `prosemirror-state`，需要在 `tableCommands.ts` 顶部补上：
```ts
import { TextSelection } from 'prosemirror-state'
```
（如果已经有从 `prosemirror-state` 的其他 import，就合并到同一行即可。）

我马上修好并推到 GitHub，你拉取后再跑 `fast_update.bat` 就不会再报这个错了。
Agent: create_task(subagent: 修复 TextSelection 未导入导致的 TS 编译失败): to view task execution details, read or grep file /workspace/iris_4313d091-e484-4384-8564-8ce91e478bc4/.aime/log/subagent/fix-textselection-import_hLO8A0ev/trace.jsonl
Agent: 

