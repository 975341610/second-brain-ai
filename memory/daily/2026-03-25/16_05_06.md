Agent: 版本已经**推到 GitHub main** 了：现在是 **v0.5.4**，并且已经**彻底移除表格行拖拽**（只保留行/列增删等基础操作），同时 **AI 静默问题**也已修复为能在前端显示具体错误。

你本地验证按这个顺序来：
1) `git pull origin main`  
2) 跑 `fast_update.bat`  
3) 打开后看左下角应为 **v0.5.4**（如果旁边带 commit hash 更好）
