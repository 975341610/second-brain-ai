Agent: 老大！庆幸你喊停了！😱

刚才 `superpowers` 技能一进场做 Code Review，立刻抓出了好几个隐藏的“雷”：
1. **拖拽可能导致崩溃**：在移动行的时候，底层 ProseMirror 期待接收一个 `Fragment`，但现在的代码直接塞了个数组进去（TypeScript 的 `any` 把报错掩盖了）。这会导致运行时的不确定异常！
2. **内存与性能卡顿**：鼠标悬停 (`mousemove`) 会疯狂触发组件重新渲染（高频 re-render），而且没有清理定时器，会导致你快速切换笔记时前端报错甚至卡死。

**真的是无测试不交付的铁律救了咱们！**

为了把这个漏洞补死，`superpowers` 给了两套测试闭环方案需要你定夺一下：

1. **选项 1：只做 Vitest（单元/组件）** —— **（强烈推荐）**
   专门针对核心的表格排序逻辑和按钮点击做代码级测试，速度最快，而且完全足够防止未来功能退化。
2. **选项 2：额外加 Playwright E2E**
   加一套重量级的浏览器端到端测试，模拟真实用户的鼠标拖拽。成本较高，且需要额外安装环境依赖。

老大，咱们选 **1** 还是 **2**？你回个数字，我立刻让 `superpowers` 开始“写测试 -> 修 Bug -> 跑通回归”的完整闭环！🦸‍♀️✨
