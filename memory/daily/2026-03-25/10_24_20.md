Agent: 老大！完美搞定！🎉 

导致 AI “装死”的世纪大 Bug 终于被拔出来了！

**核心修复总结（超能力加持版）：**
1. **彻底兼容奇葩数据源**：不仅支持标准 SSE，连那些 `sub2api` 或野鸡代理喜欢返回的直接 JSON 格式（不带 `data:` 前缀的）现在也能被精准捕获，再也不会漏掉数据了！
2. **防干等超时机制**：我加上了 **15秒“首个字符超时”保护**。如果在 15 秒内连一个字都没憋出来，前端再也不会死等，而是会直接抛出超时重试提示。
3. **打破沉默**：把网络模块的日志警告全打开了，以后上游再传什么怪异格式，都能在后台日志里看得清清楚楚。
4. **测试满分**：写了专门的验证脚本，无论是标准流还是 JSON 散装流，解析全部通过！

目前修复代码我已经妥妥地放在了一个叫 `fix/ai-stream-enhancement` 的安全分支里了。

老大，咱们是**先合并到主分支（main）你在本地跑一下试试**，还是**确认没问题了再一起让我推送到 GitHub**？听你指挥！🦸‍♀️✨
