Agent: create_task(traecli: Commit code block fix)
Agent: The frontend parser has been successfully updated to strip stray markdown backticks (e.g. ` ```python ` and ` ``` `) before inserting the code block into the Tiptap editor.

The change was applied to `nova_repo/nova-block/src/components/novablock/NovaBlockEditor.tsx` inside the `handleAIAction` event listener where the `insert_code_block` action is processed. The code text is now stripped using:
`value.replace(/^\s*```\w*\n?/, '').replace(/\n?```\s*$/, '')`

I've also dispatched a task to commit the changes locally to `nova_repo`. You can now test the API endpoint using `curl` and validate the full frontend-to-backend flow.
