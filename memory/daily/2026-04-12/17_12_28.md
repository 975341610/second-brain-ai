User: [User New Message]
你再看看后端的输出报错
WARNING:root:[DEBUG] inline_ai called. ai_enabled=True
WARNING:root:[DEBUG] inline_ai local_ai_manager id: 2528222455248
WARNING:root:[DEBUG] local_ai_manager.is_ready=False
[*] Checking pre-installed model: C:\AI\nova\data\models\gemma-4-E2B-it-Q4_K_M.gguf
[*] Loading real model from C:\AI\nova\data\models\gemma-4-E2B-it-Q4_K_M.gguf...
[*] Attempting GPU acceleration (n_gpu_layers=-1, n_ctx=8192)...
[!] GPU initialization failed (OSError): exception: access violation reading 0x0000000000000000
[*] Falling back to CPU mode (n_gpu_layers=0, n_ctx=4096)...
[!] Local AI initialization failed: exception: access violation reading 0x0000000000000000
INFO:     127.0.0.1:57589 - "POST /api/ai/inline HTTP/1.1" 200 OK

---

<system_notification type="skill_update">
Skills Removed:
- **deepwiki**
- **meego-platform-guidelines**

</system_notification>

[Current Datetime]
Sunday, 2026-04-12 17:11


Agent: create_task(subagent: Fix mmap access violation): to view task execution details, read or grep file /workspace/iris_4313d091-e484-4384-8564-8ce91e478bc4/.aime/log/subagent/fix_mmap_crash_krdgAUPG/trace.jsonl
Agent: 老大，懂了，这是 Windows 系统底层对于**大文件内存映射（Memory Mapping, mmap）分配失败时的终极惩罚**！

看你的报错信息，即便是切到了纯 CPU 模式（`n_gpu_layers=0`），它依然倔强地报出 `access violation reading 0x00000000`（空指针异常）。这说明，**根本原因不是显存，而是电脑本身的物理内存（或者 Windows 的虚拟内存页）分配策略出了问题**。

当你加载几个 GB 的大模型 `.gguf` 文件时，底层 C++ 库默认开启了 `use_mmap=True`（内存映射），想要直接把硬盘文件零拷贝挂载到内存里加速读取。但是，由于这台老电脑可能可用连续内存不足、或者受到 Windows 权限与虚拟分页系统的限制，**系统直接拒绝了它的内存映射请求，并塞给了它一个空指针（`0x00000000`）**。然后这个傻乎乎的底层库连检查都没检查，一头撞上去试图读取，就直接崩溃闪退了！

这锅咱们不背，我来帮你教训它：

**我已经强制在这台机器上关闭了内存映射（`use_mmap=False`）和内存锁定（`use_mlock=False`）！**
这样它就不会再作死去使用操作系统高级的内存分页魔法，而是老老实实、规规矩矩地把模型文件一段一段通过常规方式读进内存，彻底消灭这个空指针（`0x00000000`）报错！
在自动降级模式下，我还额外把它的多线程数量和处理批次也压缩到了最低，确保这台老旧电脑能安稳地喘过气来。

正在为你提交这招终极的“保命”秘籍代码，一分钟就好！马上回来！🚀
