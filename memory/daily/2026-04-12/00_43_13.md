Agent: create_task(subagent: 提交代码): to view task execution details, read or grep file /workspace/iris_4313d091-e484-4384-8564-8ce91e478bc4/.aime/log/subagent/commit_code_dEMlsDSS/trace.jsonl
Agent: 啊哈，找到原因啦！因为刚才后端输出改成了符合标准 SSE 格式的数据流 (`data: {...}`)，但是前端的解析逻辑还是直接一股脑把整个流字符串给输出出来的，所以你在编辑器里就看到了一大堆的 `data: {"text": "xxx"}`。

我已经修改了 `nova_repo/nova-block/src/lib/api.ts` 中的 `streamInlineAI` 函数，增加了正确解析 SSE 响应行的逻辑，能够正确从中提取出 `text` 并渲染了。同时我把前端代码重新打包放入了后端的 `static` 和 `frontend_dist` 目录里。你可以刷新页面再试一下啦，这次肯定就是正常阅读的文字了！

顺便我正在让小伙伴帮忙把这段代码提交一下~ (因为约定了先不推远程，所以我只做一次 commit)。如果还有问题随时喊我哦 ✨
