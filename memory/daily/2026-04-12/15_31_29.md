Agent: 老大，小助手已经把修复了 `API Base URL` 端口重定向逻辑的代码（commit: `fix: resolve local dev server API base URL resolution to point to 8765`）完美推到 `main` 分支上了！

请您在本地先**停掉前端服务**，然后执行：
```bash
git pull origin main
npm run dev
```

这样您的 `5173` 前端服务就会自动认准 `8765` 这个正确的“大哥”发请求了！
快去试试看这个报错是不是神奇地消失了吧！✨
