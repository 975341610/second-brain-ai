User: [User New Message]
AI还是没办法识别我的指令，它只会在笔记里生成对应的文字，为了区分普通的文字处理和编辑器指令，在我想让它做编辑器指令时，如，生成标题、标签、插入代码块等时，用【生成合适的标题】、【插入代码块】等方式

---

<system_notification type="skill_update">
Skills Removed:
- **meego-platform-guidelines**
- **deepwiki**

</system_notification>

[Current Datetime]
Sunday, 2026-04-12 07:57


Agent: create_task(traecli: 支持【】语法的 AI 编辑器强制指令)
Agent: 好的老大，明白你的意思了！现在的 AI 比较“老实”，只要不给它非常明确的强指令，它还是习惯于输出纯文本。

我们这就制定一个新的**“强指令语法协议”**：只要你的问题或要求里带了**中括号 `【】`**（比如 `【生成合适的标题】` 或 `【插入一段计算斐波那契数列的代码块】`），后端就会立刻“变脸”，给 AI 注入一条**最高优先级的强制系统规则**，命令它：“用户正在发送编辑器控制指令，**闭嘴，不要说废话，只准输出执行该操作的特定 `<Action>` 代码标签**！”

除了之前的标题和标签，我这次再帮它学会**“插入代码块 (`insert_code_block`)”**等指令的触发。这样你用 `【】` 框起来的话，它就会乖乖去调用编辑器的内部组件，而不是傻乎乎地在屏幕上打印字符了。

我现在就在后台重写这个拦截器和 Prompt 的逻辑，强化它的识别率，弄好了告诉你！✨
