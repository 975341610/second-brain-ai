Agent: 老大，这次的 `【】` 强指令问题终于排查出来并且修好了！🐛🔨

### 到底是怎么回事呢？
原来问题出在咱们的**前端数据流接收器**上。大模型（Gemma-4）其实非常听话，确实乖乖地输出了 XML 标签（比如 `<Action type="insert_code_block">...`）。
但是，流式传输（SSE）是一点一点吐出来的！有时候它会先吐出 `<Act`，过零点几秒才吐出 `ion>`。旧的正则匹配在这个“青黄不接”的瞬间傻眼了，没认出来这是个指令，就把它当成普通文字直接“啪”地一下贴到了你的笔记上。等剩下的 `ion>` 过来的时候，标签已经被拆散了，彻底失效。

### 我怎么修的？
1. **防漏网之鱼机制**：我改写了前端的 `streamBuffer` 拦截逻辑。现在如果它看到缓冲区里有一个孤零零的 `<Action` 开头，但还没收到结尾的 `</Action>`，它就会**死死捏在手里，绝对不提前放行**，直到拼凑出一个完整的指令才会触发真正的编辑器操作。
2. **多行代码块适配**：以前对于换行符支持不好，导致一旦代码块带换行就匹配失效。这次优化了正则表达式，确保不管代码有多少行都能精准打包提取！

代码我已经帮你修复、重新构建打包，并且完成本地 Commit 啦！你现在再刷新客户端去试一下 `【插入一段计算斐波那契数列的代码块】`，它绝对会秒变出一个漂漂亮亮的代码块组件！😎
