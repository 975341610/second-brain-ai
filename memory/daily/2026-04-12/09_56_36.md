User: [User New Message]
还是这样，它能输出代码，但是不能插入代码块，在代码块里写代码，甚至都不输出内联代码，只是输出纯文本

---

<system_notification type="skill_update">
Skills Removed:
- **meego-platform-guidelines**
- **deepwiki**

</system_notification>

[Current Datetime]
Sunday, 2026-04-12 09:56


Agent: create_task(traecli: Fix code block insertion in Tiptap editor)
Agent: 老大，我立马回去排查！真的是见鬼了，按理说后端的指令已经被拦截成了 `<Action>` 标签，如果不生效，那**肯定是前端在解析标签或者调用 Tiptap 插入代码块节点的 API 时掉链子了**！

我猜测有两个可能：
1. Tiptap 的 `insertContent` 插入 `codeBlock` 节点时，要求的 JSON 结构比较严苛（比如必须是 `{ type: 'codeBlock', attrs: { language }, content: [{ type: 'text', text: code }] }`），而我之前可能直接把它当成纯文本插进去了，导致它没有被渲染成真正的代码块框框。
2. 或者是流式解析器（Stream Parser）在半路把带换行的纯代码当成了普通文本截断了。

我现在已经切回开发环境去深度打卡点调试了：
- **第一步**：确认后端遇到 `【插入用c语言写的冒泡排序代码块】` 时，吐出的 `<Action>` 标签是不是百分百标准的。
- **第二步**：死磕 `NovaBlockEditor.tsx` 里解析 `type="insert_code_block"` 的那一小段逻辑，翻 Tiptap 的官方 API 文档，把它强行塞进真正的 `codeBlock` 节点里！

您稍等片刻，我这就给它修好，绝不推诿！💪
