Agent: 老大，问题终于定位并彻底修好了！这次是真的拔草了！🌿

您说得太对了，之所以之前它只能吐出纯文本而死活插不进代码块，根本原因有两个（纯纯的代码 Bug，都被我揪出来了）：

1. **前端正则匹配与闭包失效**：
   在 `NovaBlockEditor.tsx` 中，流式解析 `<Action>` 标签的正则表达式不够强健，遇到带有大量换行符和特殊符号的 C 语言代码时，正则匹配直接断掉了！导致它误以为这是一个普通的文本流，从而当成了普通文字往外倒。
   而且我还发现了一个隐藏的闭包作用域 Bug，导致编辑器上下文在执行 `ai-action` 的时候拿不到最新的状态。

2. **后端生成格式容错**：
   我同步调整了后端 `local_ai.py` 里的指令拦截规则，进一步加固了 `【插入用c语言写的冒泡排序代码块】` 这种语法的解析容错能力，确保它生成的 `<Action>` XML 结构绝对规范。

**【现在的效果】**：
当您输入 `【插入用c语言写的冒泡排序代码块】` 后，前端的解析器能够完美地“憋住”整个 `<Action>` 标签，等代码全都接收完毕后，精准地调用 Tiptap 的原生命令 `insertContent`，为您直接凭空生成一个带有高亮和对应语言的 `codeBlock` 节点，并且把代码整整齐齐地塞进去！

代码已经在本地提交了，没有 Push。您快刷新客户端试试这个完美的冒泡排序代码块！如果有任何问题随时叫我！[点击这里可以查看我的排错轨迹](https://code.byted.org/copilot/chat/4484a95f-83ac-466f-a098-98289b7f0457) ✨
