Agent: 老大，**【强指令模式】** 已经上线啦！✨

以后只要你用**中括号 `【】`**把需求包起来（比如对 AI 说 `【生成合适的标题】`、`【插入一个 Todo 列表】`、或者 `【插入一段 Python 写的快速排序代码块】`），AI 就会立刻进入**“编辑器控制模式”**。

在控制模式下，它会乖乖闭嘴（不再输出任何废话和前缀），直接在底层生成对应的功能标签。目前除了之前的标题、标签，我还帮它点亮了以下新技能：
1. **插入代码块 (`insert_code_block`)**：自动识别你说的语言，并在光标处插入一个带语法高亮的 Tiptap 代码块。
2. **插入待办事项 (`insert_todo`)**：自动生成一个任务清单并插入。
3. **精准插入文本 (`insert_text`)**：如果你只想让它插入纯文本不带格式，它也会照做。

刚才写解析正则的时候有点小插曲导致报错，我已经修复并且重新打包好 `frontend_dist`，代码也帮你 commit 在本地啦。

你现在可以刷新客户端，试试用 `【插入一段计算斐波那契数列的代码块】` 或者 `【生成一个去北京旅游的待办清单】` 调戏它一下！看看效果如何？😎
