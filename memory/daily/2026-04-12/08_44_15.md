Agent: 老大，我修好啦！🛠️

刚才让您觉得 AI 变“笨”的罪魁祸首——那个强制塞入 `insert_code_block` 的“遮羞布”代码，已经被我彻底连根拔起！

**我做了以下更新：**
1. **去除了强制代码块注入**：后端不再强行把您的所有 `【】` 指令都当成 Python 代码块。
2. **重写了系统提示词 (System Prompt)**：现在的 AI 会老老实实地根据您的真实意图动态生成 XML 动作标签了！我给它塞了详细的示例，包括：
   - 📝 `insert_text`（插入普通文本）
   - ☑️ `insert_todo`（插入待办事项）
   - 💻 `insert_code_block`（插入带语言的高亮代码块）
   - 🏷️ `set_tags`（打标签）
   - 📌 `set_title`（改标题）
3. **强制无废话模式**：AI 现在只会吐出干干净净的 `<Action>` 标签，绝不带半句废话，也没有烦人的 Markdown 包裹。

**所有的修改都已经本地 Commit（依旧没有 Push）**，提交信息是：`fix(nova-ai): remove hardcoded insert_code_block action and improve prompt for dynamic XML action parsing`。

您现在可以再测试一下啦，试试输入类似 `【帮我加一个购物清单待办】` 或者 `【用 Python 写个斐波那契】`，看看它是不是终于能聪明地识别指令，并且再也不会有残留的反引号了！期待您的反馈哦～✨
