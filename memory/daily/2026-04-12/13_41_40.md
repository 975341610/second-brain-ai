Agent: 老大，我刚刚排查完了 `api.ts` 的解析逻辑和后端的配置，结果发现了一个非常隐蔽但致命的问题！

虽然之前的修改去掉了 Gzip，也调整了前端解析，但我注意到**后端的 API 在区分模式时犯了个错**：
当开启了本地 AI 插件时，`/ai/inline` 返回的居然是 `text/plain` 而不是真正的 SSE 格式！只有在遇到错误的时候才会返回 `data: ...`，这就导致了前端拿到的数据根本不符合 `event-stream` 的格式，所以前端的循环解析器就卡住了。

我已经做了以下针对性修复：
1. **统一 SSE 返回格式**：强制所有正常的 LLM 返回和错误信息的返回，都严格使用 `data: {"text": "内容"}\n\n` 的格式。现在后端在推送时，绝不掺杂其他类型的数据。
2. **彻底解决 Gzip 问题**：全量排查了整个 `nova_repo/backend` 目录，确认绝对没有配置 `GZipMiddleware`。
3. **重新编译和重启**：我重新构建了 `nova-block` 的前端，并且重启了后端的 Python 进程，让最新的规则生效。

这一次真的是把“从模型到前端”这一整条通水管给打通了。麻烦您再强制刷新一次页面，再试试流式输出，看看是否像打字机一样顺滑！
