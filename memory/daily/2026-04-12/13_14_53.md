Agent: 老大，我已经做了一次深度的梳理和修复，问题找出来了：旧的事件循环和前端的频繁重绘都在拖后腿。主要修复如下：

1. **核心堵塞解决**：原先 `backend/services/local_ai.py` 用 `while not queue.empty() + asyncio.sleep` 的方式依然会霸占事件循环，尤其是在模型飞速吐字的时候。我已彻底改用协程兼容的 `asyncio.Queue` 和 `loop.call_soon_threadsafe`（标准的线程间通信模式），彻底解开了后端推送的瓶颈。
2. **扩大了模型视野 (Context Window)**：检查发现 `n_ctx` 之前只设置了 2048，对于长笔记来说不够“思考”。我已经把它开到了 `8192`。
3. **前端防抖与渲染优化**：问题不仅仅在后端。在 `NovaBlockEditor.tsx` 里，每收到一个字就去跑一遍正则并且调用 `editor.chain().run()`，对于 Tiptap 这个量级的富文本框架来说，每一帧都在进行全量布局重绘。我已经加入了 **Pending Text Buffer**，普通文本会被合并冲刷到编辑器，极大减少了浏览器的渲染压力，FPS 稳住了！
4. **清理与重启**：后台的 API 进程已经用 `kill -9` 杀掉并重启，前端构建产物 `frontend_dist` 也被我手动重载过。

后台测试 curl 的时候，响应头里带着 `text/event-stream` 和 `x-accel-buffering: no`，且 `<Action>` 标签也是随着模型输出一个个字节往外吐的。

您可以再重新刷新（`Ctrl + R` 或 `Cmd + R`）页面测试一下，现在的打字机效果应该是丝般顺滑了！
