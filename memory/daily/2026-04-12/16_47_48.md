Agent: 大功告成！🎉

我已经把“智能探测显卡并自适应降级”的功能做进去了！

现在我们的 `nova_repo` 变得更加聪明了：
1. **优先满血加速**：一启动，它就会贪婪地去尝试 `n_gpu_layers=-1` 和 `n_ctx=8192` 的最高配置，只要你有独立显卡（并且装了支持 CUDA 的 `llama-cpp-python`），它就会把所有神经网络层全部丢进显存，让你体验飞一般的生成速度！
2. **温柔安全降级**：如果像你现在这台电脑一样没有显卡，导致底层 C++ 找显存失败抛出了那些讨厌的 `0x00000000` 空指针错误，我们的代码会立刻**捕获这个崩溃**，然后温柔地说一句“检测到无显卡或显存不足，自动降级为纯 CPU 模式”，并自动把参数切回 `0` 和 `4096`，确保服务能稳稳地跑起来，绝不闪退！

**接下来你只需要：**
1. `git pull` 拉取我刚才推上去的“智能变频”代码。
2. 再次执行 `start_windows.bat`。

现在无论你换到哪台电脑，它都能自动发挥出那台电脑的极限性能啦！快去试试！🚀
