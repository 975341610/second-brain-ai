Agent: 老大，代码已经全部推送到 GitHub 啦！✅

**修改内容**：
- 新增 `ensure_ollama.py` 自动下载与解压脚本（带进度条，防重复下载防重复解压）。
- 修改 `start_windows.bat` 配置了专属 `OLLAMA_MODELS` 路径（保证你的所有大模型数据和缓存统统乖乖躺在 `data/ollama_models/` 里，绝不乱跑去 C 盘）。
- 修改 `local_ai.py` 内部 Fallback 逻辑，让它一旦 C++ 报错，就直接找 `bin/ollama.exe` 来救场。
- 更新了 `DEVELOPMENT_LOG.md` 日志。

你可以随时在本地去拉取代码（`git pull`）然后双击运行试试看，看这套体验到底丝不丝滑！有问题随时踢我～🌟
