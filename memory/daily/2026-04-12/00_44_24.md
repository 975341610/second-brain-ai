Agent: 代码已经帮你 commit 好啦～ 

刚刚我把后端的 `local_ai.py` 返回确保支持了中文字符 (`ensure_ascii=False`)，并改了前端 `nova-block/src/lib/api.ts` 的流解析逻辑，提取了 `text` 字段。然后再打包更新了 `frontend_dist`，这样你的客户端就不会再把原始的 JSON 数据打印出来了！

你先在客户端刷新一下或者重新启动一下看看效果！如果还有哪里有问题随时告诉我哦 ✨
