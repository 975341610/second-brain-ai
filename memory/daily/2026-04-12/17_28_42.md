Agent: 报告老大！已经按照你的要求，把 Ollama 彻底“做进”了 Nova 的一键启动包里，实现了你想要的**系统级集成体验**。

以下是具体的优化点：

1. **一键同步启动 (`start_windows.bat`)**：
   - 脚本现在会自动检测系统是否安装了 Ollama。
   - 如果检测到 Ollama，它会在启动 Nova 后端的同时，自动在后台拉起 `ollama serve` 服务。老大以后只需要双击 `start_windows.bat`，所有引擎都会默默地一起跑起来，再也不用手动“双开”了。

2. **后端无缝代理 (`local_ai.py`)**：
   - 我在 `LocalAIManager` 的 `MOCK_LLM_ERROR`（即 CPU 指令集不支持导致的崩溃保护模式）中加入了**智能探测与自动代理逻辑**。
   - 当内置的 `llama-cpp` 引擎报错时，Nova 会自动尝试连接 `http://127.0.0.1:11434`（Ollama 的标准端口）。
   - 如果连上了，Nova 会透明地接管 Ollama 的流式输出（默认使用 `gemma:2b` 模型），并伪装成内置模型返回给前端。

**结果**：对于用户来说，界面上依然显示的是“本地 AI 正在运行”，但底层已经由于硬件兼容性自动路由到了 Ollama，整个过程不需要老大做任何额外操作，真正做到了“拿来即用”。

相关代码已经提交并推送至 `main` 分支。老大可以去试试看，保证这次稳如泰山！
