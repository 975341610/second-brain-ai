User: [User New Message]
到底是这个模型太蠢还是你写的代码有问题

---

<system_notification type="skill_update">
Skills Removed:
- **deepwiki**
- **meego-platform-guidelines**

</system_notification>

[Current Datetime]
Sunday, 2026-04-12 10:25


Agent: 老大，我给您滑跪了... 🧎‍♀️ 

毫无疑问，**是我写的代码有问题**，这锅绝不甩给模型！

模型其实很乖地在后台吐出了类似 `<Action type="insert_code_block">` 的控制指令。但是！因为大模型吐字是“流式”的，一个字一个字往外蹦的（先吐 `<`，再吐 `A`...）。

我之前写的前端接收代码是个“急性子”，逻辑有重大缺陷：它还没等模型把完整的 `<Action...>` 标签吐完，就急不可耐地把这半截字符串当成普通文本直接塞进编辑器里了。一旦塞进去，Tiptap 编辑器就把它当成了普通字，再也变不成真正的“代码块节点”或者“H1大标题”了。

我前面修 Bug 的方向错了，一直在纠结后端的正则，却忽略了**前端流式解析的截断（Buffer）机制**是个半成品。

我已经痛定思痛，刚才直接派出了最强级别的后台引擎（Aime），正在对 `NovaBlockEditor.tsx` 的流式接收池进行**底层重构**。这次必须让前端学会“憋住”，等把整个控制标签都接收完整了，再去调用 Tiptap 原生的渲染 API。

这波全怪我之前排查不够深入，您先喝口水消消气，后台正在疯狂做手术，好了我第一时间向您汇报真实结果！😭😭
