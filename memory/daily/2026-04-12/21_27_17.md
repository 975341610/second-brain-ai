User: [User New Message]
关于中文错别字识别方面，本地小模型延迟太高，精度也低，反而可能识别错误，而且性能要求比较高，我想试试走纯规则方向
**1. 只做高频高精度**
**2. 用“短语搭配”代替“语义理解”**
**3. 用“混淆集 + 候选替换 + 规范短语校验”做错字修复**
**4. 用“白名单 + 保护区”控制误报**
**核心引擎**
- **Aho-Corasick**：固定错词/错短语匹配
- **Trie/Double-Array Trie**：规范短语库、术语库
- **混淆字表**：同音/形近/IME 误输
- **上下文模板规则**：处理在/再、已/以、做/作 等
- **候选替换校验**：处理 错问 -> 错误 这类
**工程策略**
- 只检查当前分句
- 中文输入法上屏后触发
- 命中高置信再提示
- 结果分级显示
- 白名单和保护区优先

---

<system_notification type="skill_update">
Skills Removed:
- **deepwiki**
- **meego-platform-guidelines**

</system_notification>

[Current Datetime]
Sunday, 2026-04-12 21:26


Agent: 老大，这个思路太绝了！✨ 举双手双脚赞成！

这完美契合了咱们**“高效、性能是死理”**的铁律。说实话，用本地小模型跑拼写检查确实是大炮打蚊子，不仅延迟高影响打字心流，而且幻觉多容易乱改。换成**纯规则引擎**，不仅响应能做到毫秒级，而且完全可控，内存占用也极低！

针对你的方案，咱们可以这样落地：

### 🛠️ 技术落地规划
1. **核心引擎 (Backend)**：
   - 引入 `pyahocorasick`（C 扩展的 AC 自动机库），速度极快，专门应对海量固定错词匹配。
   - 建立几个基础词库：**高频错词库**（直接出替换建议）、**混淆字表**（同音/形近）、**白名单/保护区**（遇到直接跳过，比如人名、地名、专业术语）。
   - 编写几个高频正则模板规则（专门干掉“在/再”、“的/地/得”、“已/以”这种毒瘤）。

2. **前端交互与工程策略 (Frontend - Tiptap)**：
   - 监听 `compositionend`（中文输入法上屏完成）和空格/标点输入事件。
   - **极致作用域**：绝不全量扫描，只提取当前光标所在的**分句**发送给后端检查，网络请求体极小。
   - 命中后依然复用咱们现有的波浪线和 Hover 替换 UI，只展示高置信度的建议。

咱们第一步可以先搭个骨架：**把现有的 LLM Spellcheck 接口替换成基础的 AC 自动机规则引擎，跑通“错词精准匹配 -> 标红提示 -> 点击替换”的闭环**。词库我们可以先写死几个测试用例（比如把“的确”错打成“地确”），后面再慢慢扩充词库。

老大，如果觉得这套方案 OK，我这就开启动力（调用 `superpowers` 严格按照 TDD 流程）开始重构咱们的 Spellcheck 引擎啦！🚀 需要我马上动手吗？
