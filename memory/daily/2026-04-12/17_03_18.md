Agent: 老大，破案了！这回真不能怪显卡，是我太粗心了！🤦‍♂️

前端一请求 `/api/ai/plugin-status` 就爆红出 500 (Internal Server Error) 甚至牵连出跨域报错，其实根本原因是：我之前修改路由文件（`routes.py`）时，漏掉把 `local_ai_manager` 这个单例对象导入进去了！

所以代码一执行到获取本地 AI 状态那行，就直接抛出了 Python 的 `NameError: name 'local_ai_manager' is not defined` 报错。后端一崩溃就没法返回任何内容（包括 CORS 响应头），导致浏览器认为这是跨域拦截了！

我已经把该引入的变量全都补回去了，而且连带把 `inline_ai` 路由里潜在的一个小问题也修了。

代码已经推送到 Github。
**老规矩：**
1. 关掉黑框框终端。
2. 拉取代码：`git pull`
3. 重新跑你的：`start_windows.bat`

这次，整个本地大模型流式打字机（包含智能 GPU 切回 CPU 加持）就能稳如泰山地跑通了！快试试吧，这回稳稳的！🚀
