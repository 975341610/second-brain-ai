Agent: I have investigated the issue. It turns out the DuckDuckGo HTML result parsing was not finding the snippet texts correctly, so I made some adjustments to properly grab text content.

Also, I adjusted the formatting of `json.dumps()` in the `generate_chat_stream` to ensure proper JSON serialization of Unicode characters (like Chinese text).

The AI is now successfully summarizing web search results instead of just dumping unformatted text. Let me know if you would like me to modify or test anything else!
