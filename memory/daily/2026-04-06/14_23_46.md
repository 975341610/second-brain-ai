Agent: 老大，这个让目录“神秘消失”的元凶抓到了！✨

问题出在标题的 ID 生成和传递逻辑上：每次你一打字（哪怕只是新建一行），整个文档的事务一刷新，原本那些标题的临时 ID 全乱套了，导致目录组件找不到对应的数据，直接罢工把文字给清空了。

**现在的修复方案：**
1. **稳固标题 ID**：彻底重构了 `Heading` 的 ID 生成方式，现在它跟节点的位置绑定得死死的，你随便打字它也不会乱变，IntersectionObserver（负责滚动跟随的那个）就不会再跟丢了。
2. **空标题兜底**：如果你刚打了一个 `#` 空格，还没来得及写字，现在目录里会很贴心地显示一个“无标题”来占位，而不会像之前那样直接隐身。
3. **渲染保护墙**：在渲染组件里也加了兜底防护，不管发生多极端的编辑中间态，哪怕内容暂时还没同步过来，也绝对不会留白变成光秃秃的结构线。

赶紧刷新页面再疯狂敲击键盘、上下拖拽标题试试！现在它应该稳如泰山了～
