Agent: 老大，搞定了！现在的按钮全部采用了极致的“图标化 + 悬停提示 (Tooltip)”方案，界面再次实现大瘦身：

1. **编辑/预览按钮重构**：原来的“编辑”和“预览”文字按钮已经完全被替换成了小巧精美的 `Pen`（笔）和 `Eye`（眼睛）图标。通过悬停提示 (`title="编辑 (Edit)"` / `title="预览 (Preview)"`) 来保证操作意图清晰，去掉了冗余文字。
2. **标签与属性区再优化**：
   - 展开/收起按钮的文字（如“+3”，“收起”）已被改写成了带 `ChevronDown`/`ChevronUp` 图标的极简胶囊，非常具有呼吸感。
   - `AI Insights` 按钮剔除了旁边长长的一串“Analyzing... / AI Insights”文字，仅保留并稍微放大了那颗灵动的 `Sparkles`（星星）图标，AI 生成建议时星星会有律动呼吸灯效果。
   - 顶部展示的“创建时间”也去掉了前面的占位字符，改为鼠标悬停时才展示完整时间的 `Clock` 小图标。
3. **顶栏状态胶囊精简**：连“正在保存”、“已加入队列”、“未保存”之类的状态文案都被干掉了。现在顶部只剩下一个极其低调小巧的纯色状态圆点（保存中是琥珀色呼吸灯、未保存是玫瑰色、已同步是翡翠绿），鼠标放上去时通过浏览器原生 tooltip 会告知你具体的最后一次同步时间和状态。

这回整体界面的干净程度可以说是“Apple 级”了，只保留了纯粹的交互图标，空间极度内收，您可以试试看效果如何！如果这种超低噪点的设计风格还顺眼，我们后续其他组件也一并往这个调性上靠～
