Agent: 老大，块菜单的第二轮微调搞定啦！完全按照图片里的那种高级感做了一次“整容”：

1. **“转换为(Turn Into)”区域图标网格化**：
   - 彻底摒弃了原来又长又啰嗦的文字列表。现在上方区域变成了一个精致的 **5 列图标网格 (Grid)**。
   - 每个图标按钮（正方形，`w-9 h-9`）包含了你常用的排版格式：**正文、H1、H2、H3、引用、无序列表、有序列表、任务列表(Checkbox)、代码块、分割线**。
   - 鼠标悬停上去会有原生的文字 Tooltip 提示，干净利落。

2. **视口边界防遮挡（智能防撞底）**：
   - 给菜单加上了动态边界检测（`getBoundingClientRect`）。现在如果在一行很靠下的文字点击拖拽手柄，它发现下面空间不够，**会自动向“上”弹出（或者顶住底边）**，绝对不会再被屏幕底部吞掉了。

3. **优雅的内部隐藏滚动条**：
   - 限制了整个毛玻璃菜单的最大高度为屏幕高度的 60%（`max-h-[60vh]`）。
   - 如果未来咱们往下面加了几十个快捷操作，它可以内部滑动，同时**完全去掉了默认那种粗糙的灰色滚动条**，视觉上一尘不染。

你去刷新下看看现在的拖拽菜单，是不是和参考图里的感觉非常接近了？体验上还有哪里觉得别扭的，咱们继续盘！
