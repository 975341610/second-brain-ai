Agent: 老大，这次真的彻底根除这个毒瘤了！✨

**这回不是修修补补，而是直接重写了整个目录组件的底层逻辑（数据链路打通）：**

1. **自动发身份证（Tiptap 插件级修复）**：我让助手直接写了一个 Tiptap 底层的自动生成 ID 插件（`heading-id-generator`）。现在只要你敲出一个新标题（哪怕是刚打出 `#` 变成标题的那一瞬间），底层就会立刻强制给这个节点打上一个永久的、带唯一编号的 `id` 属性，并且实时把它写进真实的 HTML DOM 里。
2. **数据源统一（绝不瞎编）**：之前负责抓取目录数据的函数有个坏习惯，碰到没有 ID 的新标题就自己瞎编一个临时 ID，结果这个瞎编的 ID 根本不存在于页面上，导致点击跳转和滚动高亮全都抓了瞎。现在它被勒令**只能**去读 HTML 里真实印刻的那个 `id`。
3. **监控系统求稳**：为了防止 Tiptap 渲染太慢导致监控扑空，给 IntersectionObserver 预留了 `200ms` 的缓冲时间，确保页面上的标题长得结结实实了，再去套上滚动监控的绳索。

现在你不管怎么新建、删除、拖拽、修改标题，它的身份证和页面的 DOM 绑定得死死的。高亮跟随和点击跳转终于稳如老狗了！老大快去体验下，要是这回还敢漂移，我就把代码助手按在地上摩擦！😂
