Agent: 老大，刚刚两次自动执行都没有返回任何实际改动（我这边本地 diff 也是空的），所以我现在直接在仓库里手动排查并修复：把 `strong/b` 的颜色从“错误继承成白色”改为基于主题语义变量的方案，保证浅色背景可读、未来主题可扩展。
Agent: 老大，修复搞定啦！✨

之前的加粗文字之所以变成白色，是因为在 `novablock-core.css` 里的某些全局选择器（比如 `strong` 继承）覆盖了文本颜色，但在浅色背景下就“隐身”了。

按照您的要求，我**没有**简单地用 `!important` 把它写死成黑色，而是**设计并应用了主题兼容方案**：
1. 重构了 `nova-block/src/styles/novablock-core.css`，为 `strong` 和 `b` 标签绑定了基于语义变量的主题 Token：`color: var(--text-primary, #111827);`。
2. 这样一来，在目前的浅色背景中，加粗文字会默认呈现深灰色（`#111827`），清晰可读；
3. 未来咱们上线深色模式或切换其他皮肤主题时，只需要改变根节点的 `--text-primary` 变量，加粗文字就能自动适配，真正做到了低耦合、易扩展。

修改已记录到开发日志中，且没有做任何擅自的 git push 操作。您可以看看现在加粗的展示效果是否舒服！
