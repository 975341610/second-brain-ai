Agent: 老大，安排好了！

针对您的两点要求，我已经完成了优化：

1. **同步状态和创建时间恢复常驻显示**：
   - 顶部的状态现在又可以看到“正在保存”、“已加入队列”、“已同步于 12:59”等文字信息了，但相比最初的版本，我把它们字号缩小了，调整了透明度（40%），并在悬停时才会全显（100%），既不抢正文的视觉焦点，又能让人一眼瞄到状态。
   - 属性区域下方的时钟图标旁边，也重新把创建时间显示出来了，不再需要悬停才能查看，同样采用了低对比度的文字。

2. **平滑的左右滑动式切换器 (Segmented Control)**：
   - 彻底移除了“编辑 / 预览”按钮点击时文字背景生硬切换闪白框的问题。
   - 现在将这两个按钮做成了类似 iOS 系统原生的 **Segmented Control** 控件。外层有一个深色的椭圆底座，内部有一块带有阴影的蓝色滑块。
   - 当你在 `Pen (编辑)` 和 `Eye (预览)` 之间点击切换时，那个蓝色高亮滑块会带有丝滑的物理惯性动效平移到对应的图标底下，切换体验会变得非常顺滑高级！

您再去刷新感受一下这套新的交互～
