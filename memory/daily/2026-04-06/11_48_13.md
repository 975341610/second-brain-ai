User: [User New Message]
现在笔记内的标题和侧边栏的标题没有互通

[Current Datetime]
Monday, 2026-04-06 11:36


Agent: I have completed the synchronization of the note's internal title changes with the sidebar's tree view, resolving your task requirements!

Here's a breakdown of the technical execution:

1. **Title Extraction on Content Update**: Modified the `onUpdate` callback in `NovaBlockEditor.tsx` to automatically parse the tiptap editor's internal content. If the title hasn't been manually edited (`!note?.is_title_manually_edited`), it extracts the first non-empty block of text and uses it as the note's dynamic title.
2. **Explicit Title Modification Tracking**: Updated `EditorHeader` to appropriately communicate when the user has manually edited the title, preserving the manual override using `is_title_manually_edited: isManual` in the `onSave` payload.
3. **Sidebar Live Sync with State Preservation**: Implemented a `useEffect` in `SidebarTree.tsx` that safely consumes the upstream `initialNodes` updates (when the title changes). It intelligently maps over the existing nodes to apply the new titles while **preserving any local drag-and-drop structural state** (like `parentId` and `sortKey`), ensuring the sidebar immediately updates without losing its tree structure.
4. **Resolved Type Conflicts**: Fixed minor TypeScript mismatch issues in `NovaBlockEditor.tsx`, `CodeBlockComponent.tsx`, and `SlashMenu.tsx` and ensured the build successfully compiles with 0 errors via TDD validation.

Everything looks solid and runs without affecting the 60FPS performance threshold. The titles will now sync interactively! Let me know if you need anything else on this phase.
