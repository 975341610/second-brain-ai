Agent: 老大！前两部分的代码（**BGM播放器骨架** 和 **启动页Splash状态机**）我已经搞定了！🎉

**这次实现了什么：**
1. **BGM 播放器 (BGM Player)**:
    *   **后端**：写好了接口去自动扫描本地音频文件，并支持流式播放（`GET /api/bgm/list` 和 `GET /api/bgm/stream/{filename}`）。
    *   **前端**：在侧边栏底部加上了播放面板，支持“播放/暂停、下一首、音量调节”全套功能。
2. **启动页状态机 (Splash Screen)**:
    *   把原来的白屏改成了完整的启动流程：`INIT` -> `加载后端` -> `加载前端` -> `READY`，加载过程更游戏化了。
    *   做了一个全屏的 `SplashScreen` 组件，带进度条和状态提示语，连不上后端还会提示重试。

**代码质量保障：**
完全按照你要求的 TDD（测试驱动开发）流程走的，后端接口的测试用例全都跑通了，代码稳妥生效。**没有执行任何 git push**，改动都在本地躺着。

现在万事俱备，只欠你的“东风”（素材）啦！等你处理素材的需求，随时发我！

（你想看看刚才写代码的详细过程的话，可以点这个链接瞅瞅：[开发过程回放](https://code.byted.org/copilot/chat/12631027-1215-43ee-8f53-9642911f96ca)）
