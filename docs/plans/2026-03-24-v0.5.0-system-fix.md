# v0.5.0 System Fix Implementation Plan
> **For agent:** REQUIRED SUB-SKILL: Use Section 5 to implement this plan.
**Goal:** Fix P0/P1 audit findings related to DB Sessions, Streaming, and Connection pooling in `second-brain-ai`.
**Architecture:** pywebview + FastAPI backend.
**Tech Stack:** Python, FastAPI, SQLAlchemy, HTTPX, SQLite.

### Task 1: Fix DB Session Lifecycle in `background_index_note`
**Files:**
- Modify: `second-brain-ai/backend/api/routes.py`
**Step 1:** Write a failing test `repro_session_closed.py` to reproduce the Session Closed bug.
**Step 2:** Run it to verify it fails (`sqlalchemy.exc.InvalidRequestError: This session is in the 'closed' state`).
**Step 3:** Refactor `background_index_note` to use a fresh session for the whole task and close it in `finally`.
**Step 4:** Run the reproduction test to verify it passes.
**Step 5:** Commit the changes.

### Task 2: Optimize Streaming and AIClient
**Files:**
- Modify: `second-brain-ai/backend/services/ai_client.py`
- Modify: `second-brain-ai/backend/api/routes.py`
**Step 1:** Enhance `AIClient.stream_chat` SSE parser robustness (trim lines, handle empty lines, robust [DONE] check).
**Step 2:** Add `httpx.Limits` (max_connections=100, max_keepalive_connections=50) to `AIClient` in `__init__`.
**Step 3:** Add headers `X-Accel-Buffering: no`, `Cache-Control: no-cache` to `StreamingResponse` in `routes.py`.
**Step 4:** Verify streaming with character-by-character output.
**Step 5:** Commit the changes.

### Task 3: Performance Hardening (DB Lock Retry)
**Files:**
- Modify: `second-brain-ai/backend/database.py`
- Modify: `second-brain-ai/backend/api/routes.py`
**Step 1:** Implement `with_db_retry` decorator in `database.py` (retry on `sqlalchemy.exc.OperationalError` with "locked" message).
**Step 2:** Wrap `background_index_note` logic with `with_db_retry`.
**Step 3:** Verify stability under concurrent background tasks.
**Step 4:** Commit the changes.

### Task 4: Final Verification and Report
**Step 1:** Run all existing tests in `backend/api/routes_test.py`.
**Step 2:** Perform final code review.
**Step 3:** Output the "v0.5.0 System Fix Summary Report".
